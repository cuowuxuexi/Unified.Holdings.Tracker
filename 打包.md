# Unified Holdings Tracker - 应用打包指南

> 最后更新：2025-11-17
> 当前打包就绪度：**30%** 🔴

---

## 📊 打包条件检测摘要

### 🚨 阻塞性问题（必须解决）

1. ❌ **前端未构建** - `electron/renderer/` 目录不存在
2. ❌ **packages 未构建** - domain/application/infra 模块未编译
3. ❌ **Prisma Client 未生成** - 数据库客户端缺失
4. ❌ **应用图标文件缺失** - 需要创建 icon.ico/icon.icns/icon.png

### ⚠️ 配置问题（建议解决）

5. ⚠️ **构建输出路径不一致** - vite.config.ts 与 forge.config.js 配置冲突
6. ⚠️ **electron/package.json 双重配置** - 同时存在 electron-builder 和 Electron Forge 配置
7. ⚠️ **数据库路径配置不一致** - 根目录与后端的 .env 文件路径不同
8. ⚠️ **缺少一键构建脚本** - 需要手动执行多个命令

### 💡 优化建议

- 添加打包前置检查脚本
- 配置代码签名（生产环境）
- 优化资源文件大小（fireworks.png: 1.5MB）
- 添加自动更新机制（electron-updater）

---

## 🚀 完整打包流程

### 步骤 1: 环境准备

```bash
# 确保 Node.js 版本 >= 18
node --version

# 确保所有依赖已安装
npm install
```

### 步骤 2: 生成 Prisma Client

```bash
# 在后端工作区生成 Prisma Client
npm run prisma:generate -w backend
```

**验证**：检查 `apps/backend/node_modules/.prisma/client/` 目录是否存在

### 步骤 3: 按顺序构建 packages

```bash
# 1. 构建 domain 层
cd packages/domain
npm run build
cd ../..

# 2. 构建 application 层
cd packages/application
npm run build
cd ../..

# 3. 构建 infra 层
cd packages/infra
npm run build
cd ../..
```

**验证**：检查以下目录是否存在：
- `packages/domain/dist/`
- `packages/application/dist/`
- `packages/infra/dist/`

### 步骤 4: 构建后端

```bash
npm run build -w backend
```

**验证**：检查 `apps/backend/dist/` 目录是否存在且包含 TypeScript 编译产物

### 步骤 5: 构建前端

```bash
npm run build -w frontend
```

**验证**：检查 `electron/renderer/` 目录是否存在且包含以下文件：
- `index.html`
- `assets/` 目录（包含 JS/CSS bundle）

### 步骤 6: 构建 Electron 主进程

```bash
npm run build -w electron
```

**验证**：检查 `electron/dist/main.js` 是否存在

### 步骤 7: 准备应用图标

#### Windows 图标 (.ico)

```bash
# 创建目录
mkdir electron/assets

# 准备 icon.ico 文件（256x256 或更高分辨率）
# 将 icon.ico 放置到 electron/assets/ 目录
```

#### macOS 图标 (.icns)

```bash
# macOS 上使用以下命令创建 .icns 文件
# 1. 准备 1024x1024 的 PNG 文件
# 2. 创建临时目录
mkdir MyIcon.iconset

# 3. 生成不同尺寸
sips -z 16 16     icon.png --out MyIcon.iconset/icon_16x16.png
sips -z 32 32     icon.png --out MyIcon.iconset/icon_16x16@2x.png
sips -z 32 32     icon.png --out MyIcon.iconset/icon_32x32.png
sips -z 64 64     icon.png --out MyIcon.iconset/icon_32x32@2x.png
sips -z 128 128   icon.png --out MyIcon.iconset/icon_128x128.png
sips -z 256 256   icon.png --out MyIcon.iconset/icon_128x128@2x.png
sips -z 256 256   icon.png --out MyIcon.iconset/icon_256x256.png
sips -z 512 512   icon.png --out MyIcon.iconset/icon_256x256@2x.png
sips -z 512 512   icon.png --out MyIcon.iconset/icon_512x512.png
sips -z 1024 1024 icon.png --out MyIcon.iconset/icon_512x512@2x.png

# 4. 转换为 .icns
iconutil -c icns MyIcon.iconset

# 5. 移动到 assets 目录
mv MyIcon.icns electron/assets/icon.icns
```

#### Linux 图标 (.png)

```bash
# 准备 512x512 的 PNG 文件
# 将 icon.png 放置到 electron/assets/ 目录
```

#### 临时方案（仅用于测试）

如果暂时没有设计好的图标，可以：
1. 使用在线图标生成器（如 https://icon.kitchen/）
2. 下载 Electron 默认图标模板
3. 使用占位图标（但不建议用于生产环境）

### 步骤 8: 打包应用

```bash
# 进入 electron 目录
cd electron

# 打包应用（不创建安装包）
npm run package

# 或者直接创建安装包
npm run make
```

**验证**：检查 `electron/out/` 目录：
- `package` 命令会生成应用程序目录
- `make` 命令会在 `out/make/` 生成安装包

### 步骤 9: 测试打包的应用

#### Windows

```bash
# 进入打包输出目录
cd electron/out/Portfolio Tool-win32-x64

# 运行应用
"Portfolio Tool.exe"
```

#### macOS

```bash
open "electron/out/Portfolio Tool-darwin-x64/Portfolio Tool.app"
```

#### Linux

```bash
./electron/out/Portfolio Tool-linux-x64/portfolio-tool
```

---

## 🛠️ 问题修复指南

### 问题 1: 前端未构建

**症状**：`electron/renderer/` 目录不存在

**解决**：
```bash
npm run build -w frontend
```

**根本原因**：Vite 构建未执行

---

### 问题 2: packages 未构建

**症状**：后端构建失败，提示找不到 `@uht/domain` 等模块

**解决**：
```bash
# 按顺序构建（domain 不依赖其他包，必须先构建）
cd packages/domain && npm run build && cd ../..
cd packages/application && npm run build && cd ../..
cd packages/infra && npm run build && cd ../..
```

**根本原因**：后端依赖这些 packages，必须先编译成 JavaScript

---

### 问题 3: Prisma Client 未生成

**症状**：后端运行时报错 `Cannot find module '@prisma/client'`

**解决**：
```bash
npm run prisma:generate -w backend
```

**根本原因**：Prisma Client 是根据 schema.prisma 动态生成的，不在 Git 仓库中

---

### 问题 4: 应用图标缺失

**症状**：打包失败或使用默认 Electron 图标

**解决**：
1. 准备图标文件（见步骤 7）
2. 确保文件路径正确：
   - `electron/assets/icon.ico` (Windows)
   - `electron/assets/icon.icns` (macOS)
   - `electron/assets/icon.png` (Linux)

**临时方案**：使用占位图标或下载免费图标模板

---

### 问题 5: 构建输出路径不一致

**症状**：打包时提示找不到前端资源

**当前配置**：
- `vite.config.ts`: `outDir: '../electron/renderer'`
- `forge.config.js`: `from: '../frontend/dist'`

**解决方案 A**（推荐）：修改 `electron/forge.config.js`

```javascript
// 删除或注释掉 extraResource 配置
packagerConfig: {
  asar: true,
  name: 'Portfolio Tool',
  executableName: 'PortfolioTool',
  icon: './assets/icon',
  // 删除以下配置
  // extraResource: [
  //   {
  //     from: '../frontend/dist',
  //     to: 'renderer'
  //   }
  // ]
}
```

**原因**：Vite 已经直接输出到 `electron/renderer`，不需要额外复制

**解决方案 B**：修改 `frontend/vite.config.ts`

```typescript
export default defineConfig({
  // ...
  build: {
    outDir: 'dist', // 改为输出到 frontend/dist
    // ...
  }
})
```

然后保持 `forge.config.js` 的配置不变。

---

### 问题 6: electron/package.json 双重配置

**症状**：配置混淆，不清楚使用哪个打包工具

**解决**：删除 `electron/package.json` 中的 `build` 字段

```json
{
  "name": "portfolio-tool",
  "version": "1.0.0",
  "main": "dist/main.js",
  // 删除以下 build 配置
  // "build": {
  //   "appId": "com.portfolio.tracker",
  //   ...
  // }
}
```

**原因**：项目使用 Electron Forge（`forge.config.js`），不需要 electron-builder 配置

---

### 问题 7: 数据库路径配置不一致

**症状**：开发/打包环境数据库位置不同

**当前状态**：
- 根目录 `.env`: `DATABASE_URL="file:./apps/backend/prisma/data/portfolio.db"`
- 后端 `.env`: `DATABASE_URL=file:./prisma/data/portfolio.db`

**解决**：统一使用后端目录的 `.env`

```bash
# 删除或注释根目录的 .env（如果不用于其他用途）
# 或确保后端启动时读取的是 apps/backend/.env
```

**生产环境建议**：
在 Electron 主进程中动态设置数据库路径：

```typescript
// electron/main.ts
import { app } from 'electron';
import path from 'path';

const userDataPath = app.getPath('userData');
const dbPath = path.join(userDataPath, 'portfolio.db');
process.env.DATABASE_URL = `file:${dbPath}`;
```

---

### 问题 8: 缺少一键构建脚本

**症状**：需要手动执行多个命令，容易遗漏步骤

**解决**：在根目录 `package.json` 添加以下脚本

```json
{
  "scripts": {
    "build:packages": "npm run build -w domain && npm run build -w application && npm run build -w infra",
    "build:backend": "npm run prisma:generate -w backend && npm run build -w backend",
    "build:frontend": "npm run build -w frontend",
    "build:electron": "npm run build -w electron",
    "build:all": "npm run build:packages && npm run build:backend && npm run build:frontend && npm run build:electron",
    "package": "npm run build:all && cd electron && npm run package",
    "make": "npm run build:all && cd electron && npm run make",
    "check:build": "node -e \"const fs=require('fs');const checks=['electron/renderer/index.html','apps/backend/dist','packages/domain/dist','apps/backend/node_modules/.prisma/client'];checks.forEach(p=>{console.log(p,fs.existsSync(p)?'✅':'❌')})\""
  }
}
```

**使用方法**：
```bash
# 一键构建所有模块
npm run build:all

# 构建并打包
npm run package

# 构建并生成安装包
npm run make

# 检查构建产物
npm run check:build
```

---

## 📋 快速打包清单

使用此清单确保所有步骤已完成：

- [ ] 已安装 Node.js 18+
- [ ] 已运行 `npm install`
- [ ] 已生成 Prisma Client (`npm run prisma:generate -w backend`)
- [ ] 已构建 packages/domain
- [ ] 已构建 packages/application
- [ ] 已构建 packages/infra
- [ ] 已构建后端 (`npm run build -w backend`)
- [ ] 已构建前端 (`npm run build -w frontend`)
- [ ] 已构建 Electron (`npm run build -w electron`)
- [ ] 已准备应用图标（icon.ico/icns/png）
- [ ] 已验证 `electron/renderer/` 存在
- [ ] 已验证 `apps/backend/dist/` 存在
- [ ] 已运行 `cd electron && npm run package`
- [ ] 已测试打包的应用

---

## 🎯 一键打包命令（推荐）

### 前置条件
1. 确保已添加上述"一键构建脚本"到根 `package.json`
2. 确保已准备好应用图标文件

### 执行打包

```bash
# 方式 1: 仅打包（不生成安装程序）
npm run package

# 方式 2: 打包并生成安装程序（推荐）
npm run make

# 方式 3: 手动分步执行
npm run build:all
cd electron
npm run make
```

### 打包产物位置

- **应用程序目录**：`electron/out/Portfolio Tool-win32-x64/`
- **Windows 安装包**：`electron/out/make/squirrel.windows/x64/`
- **ZIP 压缩包**：`electron/out/make/zip/win32/x64/`

---

## ⚠️ 常见问题

### Q1: 打包时提示 "Error: ENOENT: no such file or directory"

**原因**：缺少构建产物或图标文件

**解决**：
1. 运行 `npm run check:build` 检查缺失文件
2. 按照上述流程重新构建
3. 确保图标文件存在

---

### Q2: 打包后应用无法启动

**可能原因**：
1. 数据库路径错误
2. 后端服务端口被占用
3. 缺少必要的依赖

**调试方法**：
```bash
# Windows: 查看日志
%APPDATA%\Portfolio Tool\logs

# macOS: 查看日志
~/Library/Logs/Portfolio Tool
```

---

### Q3: 打包后应用体积过大

**原因**：包含了 node_modules 中的 devDependencies

**解决**：
1. 确保 devDependencies 分类正确
2. 使用 `npm prune --production` 删除开发依赖
3. 检查 `forge.config.js` 的 `ignore` 配置

---

### Q4: macOS 上打包提示需要签名

**原因**：macOS 10.15+ 要求应用签名

**解决**：
1. 开发环境：在系统偏好设置中允许运行未签名应用
2. 生产环境：申请 Apple Developer 账号并配置签名证书

```javascript
// electron/forge.config.js
packagerConfig: {
  osxSign: {
    identity: 'Developer ID Application: Your Name (TEAM_ID)',
    hardenedRuntime: true,
    entitlements: 'entitlements.plist',
    'entitlements-inherit': 'entitlements.plist',
  },
  osxNotarize: {
    appleId: 'your-apple-id@example.com',
    appleIdPassword: '@keychain:AC_PASSWORD',
  }
}
```

---

## 📝 版本发布流程

### 1. 更新版本号

```bash
# 在 electron/package.json 中更新 version
npm version patch -w electron  # 1.0.0 -> 1.0.1
npm version minor -w electron  # 1.0.0 -> 1.1.0
npm version major -w electron  # 1.0.0 -> 2.0.0
```

### 2. 构建和打包

```bash
npm run make
```

### 3. 测试安装包

- Windows: 运行 `.exe` 安装程序
- macOS: 挂载 `.dmg` 并拖拽安装
- Linux: 安装 `.deb` 或 `.rpm`

### 4. 创建 Release

```bash
# 提交更改
git add .
git commit -m "chore: release v1.0.1"
git tag v1.0.1
git push origin main --tags
```

### 5. 上传安装包

将 `electron/out/make/` 中的安装包上传到：
- GitHub Releases
- 自建下载服务器
- CDN

---

## 🔧 高级配置

### 自动更新配置

```bash
# 安装 electron-updater
npm install electron-updater -w electron
```

```typescript
// electron/main.ts
import { autoUpdater } from 'electron-updater';

autoUpdater.checkForUpdatesAndNotify();

autoUpdater.on('update-available', () => {
  console.log('Update available');
});

autoUpdater.on('update-downloaded', () => {
  autoUpdater.quitAndInstall();
});
```

### 代码签名配置

#### Windows (需要证书)

```javascript
// electron/forge.config.js
makers: [
  {
    name: '@electron-forge/maker-squirrel',
    config: {
      certificateFile: './cert.pfx',
      certificatePassword: process.env.CERT_PASSWORD,
    }
  }
]
```

#### macOS

参见 Q4 的签名配置

---

## 📚 参考资源

- [Electron Forge 文档](https://www.electronforge.io/)
- [Electron 打包指南](https://www.electronjs.org/docs/latest/tutorial/application-distribution)
- [Vite 构建配置](https://vitejs.dev/config/build-options.html)
- [Prisma 部署指南](https://www.prisma.io/docs/guides/deployment/deployment-guides)

---

## 🆘 需要帮助？

如遇到打包问题，请检查：
1. 本文档的"常见问题"章节
2. `electron/out/` 目录下的错误日志
3. 运行 `npm run check:build` 验证构建状态

如问题仍未解决，请创建 Issue 并附上：
- 错误信息截图
- 执行的命令
- 系统环境（操作系统、Node.js 版本）
