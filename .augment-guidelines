# Unified Holdings Tracker — Augment 指南

## 目录结构

```
d:\Unified.Holdings.Tracker-main\
├── apps/
│   └── backend/              # 后端 Express 服务 (端口 3001)
│       ├── prisma/           # Prisma schema + SQLite 数据库
│       ├── src/
│       │   ├── routes/       # API 路由
│       │   ├── services/     # 业务服务 + 计算引擎
│       │   ├── config/       # 配置管理
│       │   └── server.ts     # 服务器入口
│       └── package.json
├── packages/
│   ├── domain/               # 领域层 — 实体、值对象、仓储接口
│   ├── application/          # 应用层 — Use Cases
│   └── infra/                # 基础设施层 — Prisma 仓储、缓存、API 调用
├── frontend/                 # 前端 React 19 + Vite + Ant Design
│   ├── src/
│   │   ├── app/              # 路由配置
│   │   ├── components/       # UI 组件
│   │   ├── features/         # 功能模块
│   │   ├── shared/           # 共享代码
│   │   ├── store/            # Zustand 状态管理
│   │   └── main.tsx          # 入口
│   └── package.json
├── electron/                 # Electron 主进程
│   ├── launcher/             # 启动器
│   ├── forge.config.js       # Electron Forge 配置
│   └── package.json
├── openspec/                 # OpenSpec 变更提案管理
├── docs/                     # 项目文档
└── deploy/                   # 部署配置 (nginx, setup.sh)
```

## 架构模式

- **Monorepo + DDD 分层**: Domain → Application → Infrastructure
- **前后端分离**: 后端 RESTful API，前端 Axios 调用
- **Electron 集成**: 主进程管理窗口和后端服务

## 技术栈

- Electron 35 + React 19 + Express 5 + TypeScript
- Prisma ORM + SQLite
- Ant Design 5 + Zustand + TanStack Query
- npm workspaces

## 开发命令

```bash
npm install                     # 安装依赖
npm run dev                     # 并行启动前后端
npm run dev:backend             # 后端 http://localhost:3001
npm run dev:frontend            # 前端 http://localhost:5173
npm run test:backend            # 后端测试 (Jest)
npm run test -w frontend        # 前端测试 (Vitest)
```

## 注意事项

- 分层约束：Domain 不依赖外部库 → Application 只依赖 Domain → Infra 实现 Domain 接口
- 财务数据使用 `Decimal` 类型，禁止 `Float`
- 仅支持 SQLite（桌面嵌入式应用）
- 涉及架构变更时，遵循 OpenSpec 工作流（`openspec/` 目录）
