# 投资组合管理系统 - 周期收益计算优化方案

> 最后更新：2025-11-17

---

## 📊 研究发现总结

### 现状分析

**优势：**
- ✅ 已实现高质量的 FIFO 成本计算
- ✅ 使用 Modified Dietz 法计算周期收益（适合个人投资组合）
- ✅ 清晰分离已实现/未实现盈亏
- ✅ 支持多币种（CNY/USD/HKD）和汇率转换

**核心问题：**
1. **基准日期不合理**：周度使用"本周一"而非"上周五"，遇假期无数据
2. **回溯窗口不足**：周5天、月10天、年30天，无法应对春节/国庆等长假
3. **缺乏缓存机制**：每次请求都调用 API，性能差且易触发限流
4. **前端状态混淆**：无法区分"数据缺失"和"涨幅为0"

---

## 🎯 优化目标

- 周度/月度收益数据可用性从当前 ~60% 提升至 **95%+**
- API 调用次数减少 **90%+**（通过多级缓存）
- 页面加载时间控制在 **2秒内**
- 清晰区分数据状态（缺失 vs 零涨幅）

---

## 📐 核心算法设计

### 1. 基准日期改进

**当前问题：**
```typescript
// 周度收益使用"本周一"作为基准
const monday = startOfWeek(today, { weekStartsOn: 1 });
```
❌ 如果本周一是假期/停牌，无K线数据导致失败

**优化方案：**
```typescript
// 改用"上周五"（上周最后一个交易日）
function getLastWeekFriday(today: Date): Date {
  const dayOfWeek = today.getDay(); // 0=周日, 6=周六
  const daysSinceMonday = (dayOfWeek + 6) % 7; // 周一=0, 周日=6
  const daysToLastFriday = daysSinceMonday + 3;
  return subDays(today, daysToLastFriday);
}
```

**各周期基准日期：**

| 周期 | 当前基准 | 优化后基准 | 说明 |
|-----|---------|-----------|------|
| 周度 | 本周一 | **上周五** | 上周最后交易日 |
| 月度 | 本月1日 | **上月最后一个交易日** | 通常是上月最后工作日 |
| 年度 | 本年1月1日 | **去年最后一个交易日** | 通常是去年12月31日或倒数第二个工作日 |

### 2. 智能回溯窗口

**当前问题：**
- 周度回溯5天 → 无法覆盖长假（如国庆7天）
- 月度回溯10天 → 月末遇长假可能失败
- 年度回溯30天 → 春节假期可能导致数据缺失

**优化方案：**

```typescript
// 扩大回溯天数以应对长假
const LOOKBACK_DAYS = {
  week: 15,   // 5天 → 15天 (覆盖双周 + 缓冲)
  month: 20,  // 10天 → 20天 (覆盖月末长假)
  year: 60    // 30天 → 60天 (覆盖春节前后)
};
```

**回溯逻辑：**
```typescript
async function getBasePrice(
  code: string,
  anchorDate: Date,
  maxLookbackDays: number,
  cacheTTL: number = 3600
): Promise<{ price: number | null; date: string | null }> {
  // 1. 检查缓存
  const cacheKey = `base-price:${code}:${formatDate(anchorDate)}:${maxLookbackDays}`;
  const cached = cacheService.get(cacheKey);
  if (cached) return cached;

  // 2. 获取K线数据（向前回溯）
  const startDate = subDays(anchorDate, maxLookbackDays);
  const klineData = await fetchKline(code, 'daily', formatDate(startDate), formatDate(anchorDate), 'qfq');

  // 3. 从最近日期向前查找有效数据
  const sortedKline = klineData.sort((a, b) => b.date.localeCompare(a.date));
  for (const point of sortedKline) {
    if (point.close && !isNaN(point.close)) {
      const result = { price: point.close, date: point.date };
      cacheService.set(cacheKey, result, cacheTTL);
      return result;
    }
  }

  // 4. 未找到有效数据
  return { price: null, date: null };
}
```

### 3. 多级缓存策略

**缓存架构：**

```
L1: 内存缓存 (node-cache) - 已实现
  ├─ K线数据: 15分钟 TTL
  ├─ 实时行情: 30秒 TTL
  └─ 新增缓存项 ↓

L1+: 增强内存缓存 (本次实施)
  ├─ 基准价格 (周): 1小时 TTL
  ├─ 基准价格 (月): 6小时 TTL
  ├─ 基准价格 (年): 24小时 TTL
  └─ 周期统计结果: 5分钟 TTL

L2: 数据库快照 (未来扩展)
  └─ PerformanceSnapshot 表
      └─ 每日收盘后生成快照
```

**缓存配置：**

```typescript
const CACHE_CONFIG = {
  // 现有配置
  kline: {
    ttl: 15 * 60 * 1000,  // 15分钟
    maxKeys: 1000
  },
  quote: {
    ttl: 30 * 1000,       // 30秒
    maxKeys: 500
  },

  // 新增配置
  basePrice: {
    week: 60 * 60 * 1000,        // 1小时
    month: 6 * 60 * 60 * 1000,   // 6小时
    year: 24 * 60 * 60 * 1000    // 24小时
  },
  periodStats: {
    ttl: 5 * 60 * 1000,   // 5分钟
    maxKeys: 100
  }
};
```

**缓存键设计：**

```typescript
// 基准价格缓存键
`base-price:${code}:${anchorDate}:${maxLookbackDays}`
// 示例：base-price:sh600519:2025-11-15:15

// 周期统计缓存键
`stats:${portfolioId}:${period}:${timeBucket}`
// 示例：stats:uuid-123:weekly:12345 (timeBucket = Math.floor(Date.now() / 300000))
```

### 4. Modified Dietz 收益率计算

**保持现有算法**（已验证准确性）：

```typescript
// 公式
periodReturnPercent = (endValue - startValue - cashFlows) / (startValue + cashFlows * weight)

// 其中：
// - endValue: 期末总资产（现金 + 持仓市值）
// - startValue: 期初总资产
// - cashFlows: 期间净现金流（DEPOSIT - WITHDRAW）
// - weight: 现金流时间权重（简化版使用 0.5，假设发生在期中）
```

**优化点：**
1. 期末优先使用实时行情（`useRealtimeEndValue: true`）
2. 期初/期末无K线数据时，使用成本价估算
3. 支持时间加权现金流（可选，精度更高）

---

## 🏗️ 数据结构设计

### 推荐方案：缓存增强型（无需修改 Prisma Schema）

**优点：**
- ✅ 实施快速（1-2天）
- ✅ 风险低，易回滚
- ✅ 性能提升显著（90%+ API调用减少）
- ✅ 无需数据库迁移

**架构图：**

```
┌─────────────┐
│  前端请求    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────┐
│  API: /api/portfolio/:id     │
└──────────┬──────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  calculatePeriodStats()           │
│  ├─ 获取基准价格 (缓存优先)        │
│  ├─ 重建期初/期末持仓状态          │
│  ├─ 计算资产估值                  │
│  └─ Modified Dietz 公式计算        │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  PeriodCacheService               │
│  ├─ getBasePrice()                │
│  │   ├─ 检查缓存 → 命中返回       │
│  │   ├─ 调用 fetchKline()         │
│  │   ├─ 向前查找有效收盘价         │
│  │   └─ 缓存结果                  │
│  └─ getPeriodStats()              │
│      └─ 缓存整体计算结果           │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│  腾讯财经API                      │
│  (带请求去重和并发控制)            │
└───────────────────────────────────┘
```

### 核心组件设计

#### 1. PeriodCacheService

**文件路径：** `packages/infra/src/cache/period-cache-service.ts`

```typescript
export class PeriodCacheService {
  private cache = cacheService; // 复用现有缓存

  /**
   * 获取周期基准价格（带缓存）
   */
  async getBasePrice(
    code: string,
    anchorDate: Date,
    period: 'week' | 'month' | 'year'
  ): Promise<{ price: number | null; date: string | null }> {
    const dateStr = format(anchorDate, 'yyyy-MM-dd');
    const cacheKey = `base-${period}:${code}:${dateStr}`;

    // 检查缓存
    const cached = this.cache.get<{ price: number; date: string }>(cacheKey);
    if (cached) {
      console.log(`[PeriodCache] 缓存命中: ${cacheKey}`);
      return cached;
    }

    // 计算回溯天数和缓存TTL
    const config = {
      week: { lookback: 15, ttl: 3600 },
      month: { lookback: 20, ttl: 21600 },
      year: { lookback: 60, ttl: 86400 }
    }[period];

    // 获取基准价格
    const result = await getBasePrice(code, anchorDate, config.lookback, config.ttl);
    return result;
  }

  /**
   * 获取周期统计（带缓存）
   */
  async getPeriodStats(
    portfolioId: string,
    period: 'daily' | 'weekly' | 'monthly' | 'yearly',
    quotes?: Record<string, Quote>
  ) {
    // 使用时间桶避免高频计算
    const timeBucket = Math.floor(Date.now() / 300000); // 5分钟桶
    const cacheKey = `stats:${portfolioId}:${period}:${timeBucket}`;

    const cached = this.cache.get(cacheKey);
    if (cached) return cached;

    // 计算并缓存
    const stats = await calculatePeriodStats(/* ... */);
    this.cache.set(cacheKey, stats, CACHE_CONFIG.periodStats.ttl);
    return stats;
  }
}

export const periodCacheService = new PeriodCacheService();
```

#### 2. 优化的基准价格函数

**文件路径：** `apps/backend/src/services/calculationService.ts`

```typescript
/**
 * 获取周基准价格（上周五）
 */
export async function getWeekBasePrice(code: string, today: Date = new Date()) {
  const dayOfWeek = today.getDay(); // 0=周日, 6=周六
  const daysSinceMonday = (dayOfWeek + 6) % 7; // 周一=0, 周日=6
  const daysToLastFriday = daysSinceMonday + 3;
  const lastWeekFriday = subDays(today, daysToLastFriday);

  return getBasePrice(code, lastWeekFriday, 15, 3600); // 15天回溯，1小时缓存
}

/**
 * 获取月基准价格（上月最后交易日）
 */
export async function getMonthBasePrice(code: string) {
  const today = new Date();
  const firstDayOfMonth = new Date(today.getFullYear(), today.getMonth(), 1);
  const lastDayOfPrevMonth = subDays(firstDayOfMonth, 1);

  return getBasePrice(code, lastDayOfPrevMonth, 20, 21600); // 20天回溯，6小时缓存
}

/**
 * 获取年基准价格（去年最后交易日）
 */
export async function getYearBasePrice(code: string) {
  const today = new Date();
  const firstDayOfYear = new Date(today.getFullYear(), 0, 1);
  const lastDayOfPrevYear = subDays(firstDayOfYear, 1);

  return getBasePrice(code, lastDayOfPrevYear, 60, 86400); // 60天回溯，24小时缓存
}

/**
 * 智能获取基准价格（增强版）
 */
export async function getBasePrice(
  code: string,
  anchorDate: Date,
  maxLookbackDays: number,
  cacheTTL: number = 3600
): Promise<{ price: number | null; date: string | null }> {
  const dateStr = formatDate(anchorDate);
  const cacheKey = `base-price:${code}:${dateStr}:${maxLookbackDays}`;

  // 检查缓存
  const cached = cacheService.get<{ price: number; date: string }>(cacheKey);
  if (cached) {
    console.log(`✓ [getBasePrice] 缓存命中: ${code} @ ${cached.date} = ${cached.price}`);
    return cached;
  }

  console.log(`⌛ [getBasePrice] 获取K线: ${code}, 目标日期=${dateStr}, 回溯=${maxLookbackDays}天`);

  // 获取K线数据
  const startDateStr = formatDate(subDays(anchorDate, maxLookbackDays));
  const klineData = await fetchKline(code, 'daily', startDateStr, dateStr, 'qfq');

  if (!klineData || klineData.length === 0) {
    console.warn(`✗ [getBasePrice] ${code} 无K线数据`);
    return { price: null, date: null };
  }

  // 从最近日期向前查找有效数据
  const sortedKline = [...klineData].sort((a, b) => b.date.localeCompare(a.date));
  for (const point of sortedKline) {
    if (point.close && !isNaN(point.close)) {
      const daysDiff = differenceInDays(anchorDate, parseISO(point.date));
      const result = { price: point.close, date: point.date };

      if (daysDiff > 0) {
        console.log(`⚠ [getBasePrice] ${code} 目标日期${dateStr}无数据，使用${point.date}的价格（回溯${daysDiff}天）`);
      } else {
        console.log(`✓ [getBasePrice] ${code} @ ${point.date} = ${point.close}`);
      }

      // 缓存结果
      cacheService.set(cacheKey, result, cacheTTL);
      return result;
    }
  }

  console.error(`✗ [getBasePrice] ${code} 在${maxLookbackDays}天窗口内无有效收盘价`);
  return { price: null, date: null };
}
```

#### 3. 前端状态区分

**文件路径：** `frontend/src/components/PortfolioSummary.tsx`

```typescript
/**
 * 渲染周期收益（区分三种状态）
 */
const renderPeriodReturn = (
  amount: number | null | undefined,
  percent: number | null | undefined,
  label: string
) => {
  // 状态1: 数据缺失
  if (percent === null || percent === undefined) {
    return (
      <Tooltip title={`${label}数据暂不可用，可能因为假期、停牌或新上市股票`}>
        <div>
          <div style={{ fontWeight: 700, color: '#999', fontSize: '14px' }}>N/A</div>
          <div style={{ fontSize: '11px', color: '#bbb' }}>数据缺失</div>
        </div>
      </Tooltip>
    );
  }

  // 状态2: 零涨幅（真实的0%）
  if (percent === 0 && amount === 0) {
    return (
      <div>
        <div style={{ fontWeight: 700, color: '#888', fontSize: '14px' }}>0.00</div>
        <div style={{ fontSize: '11px', color: '#888' }}>0.00%</div>
      </div>
    );
  }

  // 状态3: 正常涨跌
  const color = amount! > 0 ? '#f5222d' : amount! < 0 ? '#52c41a' : '#888';
  const prefix = amount! > 0 ? '+' : '';

  return (
    <div>
      <div style={{ fontWeight: 700, color, fontSize: '14px' }}>
        {prefix}{formatNumber(amount)}
      </div>
      <div style={{ fontSize: '11px', color }}>
        {prefix}{formatPercent(percent)}
      </div>
    </div>
  );
};

// 使用示例
<div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 8 }}>
  <Text type="secondary">周度收益:</Text>
  {renderPeriodReturn(weeklyReturnAmount, weeklyReturnPercent, '周度')}
</div>
<div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 4 }}>
  <Text type="secondary">月度收益:</Text>
  {renderPeriodReturn(monthlyReturnAmount, monthlyReturnPercent, '月度')}
</div>
```

---

## 🚀 性能优化策略

### 1. API调用优化

#### 1.1 请求去重

**问题：** 短时间内多次请求相同数据

**解决方案：**
```typescript
const pendingRequests = new Map<string, Promise<any>>();

async function fetchKlineDeduped(code: string, ...args) {
  const key = `${code}:${args.join(':')}`;

  if (pendingRequests.has(key)) {
    console.log(`[fetchKline] 复用进行中的请求: ${key}`);
    return pendingRequests.get(key);
  }

  const promise = fetchKline(code, ...args).finally(() => {
    pendingRequests.delete(key);
  });

  pendingRequests.set(key, promise);
  return promise;
}
```

#### 1.2 并发控制

**问题：** 大量资产同时请求可能触发API限流

**解决方案：**
```typescript
import pLimit from 'p-limit';

const apiLimit = pLimit(5); // 最多5个并发请求

async function batchFetchKlines(codes: string[], period: string, startDate: string, endDate: string) {
  const tasks = codes.map(code =>
    apiLimit(() => fetchKlineDeduped(code, period, startDate, endDate, 'qfq'))
  );
  return Promise.all(tasks);
}
```

#### 1.3 智能预加载

**策略：** 用户打开投资组合页面时，后台预加载周/月基准价格

```typescript
async function preloadBasePrices(assetCodes: string[]) {
  const tasks = assetCodes.flatMap(code => [
    getWeekBasePrice(code),
    getMonthBasePrice(code),
    getYearBasePrice(code)
  ]);

  // 后台并发加载，不阻塞主流程
  Promise.all(tasks).catch(err =>
    console.warn('[preloadBasePrices] Failed:', err)
  );
}

// 在 GET /api/portfolio/:id 路由中调用
router.get('/:id', async (req, res) => {
  const portfolio = await getPortfolio(req.params.id);
  const assetCodes = extractAssetCodes(portfolio.transactions);

  // 异步预加载（不阻塞响应）
  preloadBasePrices(assetCodes);

  res.json(portfolio);
});
```

### 2. 前端优化

#### 2.1 React Query 缓存

```typescript
// frontend/src/hooks/usePortfolioStats.ts
import { useQuery } from '@tanstack/react-query';

export function usePortfolioStats(portfolioId: string) {
  return useQuery({
    queryKey: ['portfolio-stats', portfolioId],
    queryFn: () => fetchPortfolioStats(portfolioId),
    staleTime: 5 * 60 * 1000,    // 5分钟内数据视为新鲜
    cacheTime: 30 * 60 * 1000,   // 缓存30分钟
    refetchInterval: 60 * 1000,  // 每分钟自动刷新
    refetchOnWindowFocus: true   // 窗口重新获得焦点时刷新
  });
}
```

#### 2.2 骨架屏

```typescript
function PortfolioSummary({ portfolioId }) {
  const { data: stats, isLoading } = usePortfolioStats(portfolioId);

  if (isLoading) {
    return <PortfolioSummarySkeleton />; // 显示灰色占位块
  }

  return (
    <Card>
      {/* 实际内容 */}
    </Card>
  );
}

// 骨架屏组件
function PortfolioSummarySkeleton() {
  return (
    <Card>
      <Skeleton active paragraph={{ rows: 4 }} />
    </Card>
  );
}
```

---

## 📋 实施计划

### Phase 1: 快速修复 (1-2天) ⭐ 优先级最高

**目标：** 解决周度/月度收益显示为0或N/A的问题

**文件修改清单：**

1. **`apps/backend/src/services/calculationService.ts`**
   - ✅ 修改 `getWeekBasePrice()`: 改用上周五，回溯15天
   - ✅ 修改 `getMonthBasePrice()`: 改用上月末，回溯20天
   - ✅ 修改 `getYearBasePrice()`: 回溯60天
   - ✅ 增强 `getBasePrice()`: 添加详细日志

2. **`frontend/src/components/PortfolioSummary.tsx`**
   - ✅ 添加 `renderPeriodReturn()` 函数
   - ✅ 区分 null/undefined/0 三种状态
   - ✅ 添加 Tooltip 显示基准日期和原因

**测试场景：**
- A股：茅台 (sh600519)、招商银行 (sh600036)、宁德时代 (sz300750)
- 港股：腾讯 (hk00700)、阿里巴巴 (hk09988)、美团 (hk03690)
- 美股：苹果 (AAPL)、微软 (MSFT)、特斯拉 (TSLA)
- 覆盖节假日：春节（2月）、国庆（10月）、元旦（1月）

**验收标准：**
- [ ] 周度收益数据可用性 > 90%
- [ ] 月度收益数据可用性 > 95%
- [ ] 前端正确区分数据缺失和零涨幅
- [ ] Tooltip 显示准确的基准日期

**交付物：**
- 修改后的源代码
- 测试报告（包含截图）

---

### Phase 2: 性能优化 (2-3天)

**目标：** 减少API调用，提升响应速度

**文件修改/新增清单：**

1. **新增 `packages/infra/src/cache/period-cache-service.ts`**
   - 实现 PeriodCacheService 类
   - 管理基准价格缓存
   - 管理周期统计缓存

2. **修改 `apps/backend/src/services/calculationService.ts`**
   - 集成 PeriodCacheService
   - 实现请求去重（pendingRequests Map）
   - 实现并发控制（p-limit）

3. **修改 `apps/backend/src/routes/portfolio.ts`**
   - 添加预加载逻辑
   - 优化响应时间

4. **新增 `frontend/src/hooks/usePortfolioStats.ts`**
   - 使用 React Query
   - 配置缓存策略

5. **修改 `frontend/package.json`**
   - 添加依赖：`@tanstack/react-query`

**性能测试：**

| 指标 | 优化前 | 目标 | 验证方法 |
|-----|-------|------|---------|
| API调用次数 | ~30次/页面 | <3次/页面 | Chrome DevTools Network |
| 首次加载时间 | ~5秒 | <2秒 | Performance API |
| 后续加载时间 | ~3秒 | <500ms | React Query Devtools |
| 缓存命中率 | 0% | >90% | 日志统计 |

**验收标准：**
- [ ] API调用次数减少 > 90%
- [ ] 首次加载时间 < 2秒
- [ ] 后续加载时间 < 500ms
- [ ] 缓存命中率 > 90%

**交付物：**
- 新增/修改的源代码
- 性能对比报告（含图表）
- 更新的架构文档

---

### Phase 3: 用户体验提升 (1-2天)

**目标：** 清晰区分数据状态，改进加载体验

**UI/UX 改进清单：**

1. **骨架屏（Skeleton）**
   - 替代 Loading 转圈
   - 保持布局稳定性

2. **Tooltip 增强**
   - 显示基准日期（如"上周五 2025-11-15"）
   - 显示数据来源（实时行情/K线数据）
   - 显示缓存状态（可选，开发模式）

3. **手动刷新按钮**
   - 允许用户强制刷新数据
   - 显示刷新时间戳

4. **错误边界处理**
   - 优雅降级（部分数据失败不影响整体）
   - 错误提示友好化

**文件修改清单：**

1. **`frontend/src/components/PortfolioSummary.tsx`**
   - 添加骨架屏组件
   - 增强 Tooltip 内容
   - 添加刷新按钮

2. **`frontend/src/components/ErrorBoundary.tsx`** (新增)
   - 实现错误边界组件

3. **`frontend/src/utils/formatters.ts`**
   - 添加日期格式化工具函数

**验收标准：**
- [ ] 骨架屏替代所有 Loading 状态
- [ ] Tooltip 显示完整信息（基准日期、数据来源）
- [ ] 手动刷新功能正常
- [ ] 错误边界能捕获并友好显示错误

**交付物：**
- 修改后的前端组件
- UI/UX 设计文档（含截图对比）

---

### Phase 4: 测试与文档 (1天)

**目标：** 确保代码质量和可维护性

**任务清单：**

1. **单元测试**
   - `calculationService.test.ts`: 测试基准价格函数
   - `period-cache-service.test.ts`: 测试缓存逻辑
   - 目标覆盖率：80%+

2. **集成测试**
   - API端到端测试
   - 模拟假期场景
   - 模拟缓存命中/未命中

3. **文档更新**
   - 更新 `CLAUDE.md`（架构文档）
   - 更新 `README.md`（用户手册）
   - 添加 API 文档注释

4. **Code Review**
   - 代码规范检查（ESLint）
   - 类型检查（TypeScript）
   - 性能 profiling

**测试用例示例：**

```typescript
  // apps/backend/src/services/__tests__/calculationService.test.ts
  describe('getWeekBasePrice', () => {
    it('应返回上周五的价格', async () => {
      // 假设今天是 2025-11-17 (周一)
      const today = new Date('2025-11-17');
      const result = await getWeekBasePrice('sh600519', today);

    // 上周五是 2025-11-14
    expect(result.date).toBe('2025-11-14');
    expect(result.price).toBeGreaterThan(0);
  });

  it('应处理长假场景（回溯15天）', async () => {
    // 假设上周五是国庆假期
    const result = await getWeekBasePrice('sh600519', new Date('2025-10-08'));

    // 应回溯到节前最后一个交易日 (2025-09-30)
    expect(result.date).toBe('2025-09-30');
  });

  it('应使用缓存', async () => {
    const spy = jest.spyOn(global, 'fetch');

    // 第一次调用
    await getWeekBasePrice('sh600519');
    const firstCallCount = spy.mock.calls.length;

    // 第二次调用（应命中缓存）
    await getWeekBasePrice('sh600519');
    const secondCallCount = spy.mock.calls.length;

    expect(secondCallCount).toBe(firstCallCount); // 无新增API调用
  });
});
```

**验收标准：**
- [ ] 单元测试覆盖率 > 80%
- [ ] 集成测试通过率 100%
- [ ] ESLint 0 errors, 0 warnings
- [ ] TypeScript 编译无错误
- [ ] 文档完整且准确

**交付物：**
- 测试代码和覆盖率报告
- 更新的技术文档
- Code Review 总结

---

## 🔮 未来扩展方案（可选）

### 方案：数据库快照表

**适用场景：**
- 用户量增长后，需要长期历史对比
- 需要生成月度/年度报表
- 需要实现收益曲线图
- 需要支持组合对比分析

**实施时机：** 3-6个月后，根据用户需求评估

### Schema 设计

```prisma
// apps/backend/prisma/schema.prisma

model PerformanceSnapshot {
  id              String   @id @default(uuid())
  portfolioId     String
  snapshotDate    DateTime // 快照日期（每日收盘时生成）
  snapshotType    String   // 'daily' | 'weekly' | 'monthly' | 'yearly'

  // 资产状态
  totalValue      Decimal  @db.Decimal(20, 2) // 总资产（市值 + 现金）
  cashBalance     Decimal  @db.Decimal(20, 2) // 现金余额
  marketValue     Decimal  @db.Decimal(20, 2) // 持仓市值

  // 盈亏数据
  totalPnl        Decimal  @db.Decimal(20, 2) // 总盈亏
  realizedPnl     Decimal  @db.Decimal(20, 2) // 已实现盈亏
  unrealizedPnl   Decimal  @db.Decimal(20, 2) // 未实现盈亏

  // 收益率（相对不同基准）
  dailyReturn     Decimal? @db.Decimal(10, 6) // 日收益率
  weeklyReturn    Decimal? @db.Decimal(10, 6) // 周收益率
  monthlyReturn   Decimal? @db.Decimal(10, 6) // 月收益率
  yearlyReturn    Decimal? @db.Decimal(10, 6) // 年收益率
  totalReturn     Decimal? @db.Decimal(10, 6) // 累计收益率

  // 元数据
  createdAt       DateTime @default(now())
  portfolio       Portfolio @relation(fields: [portfolioId], references: [id], onDelete: Cascade)

  @@index([portfolioId, snapshotDate])
  @@index([portfolioId, snapshotType, snapshotDate])
  @@unique([portfolioId, snapshotDate])
}

model Portfolio {
  // ... 现有字段 ...
  snapshots       PerformanceSnapshot[]
}
```

### 定时任务实现

```typescript
// apps/backend/src/jobs/daily-snapshot.job.ts
import { CronJob } from 'cron';

/**
 * 每日快照任务
 * 执行时间：每个交易日晚上 20:00
 */
export const dailySnapshotJob = new CronJob('0 20 * * 1-5', async () => {
  console.log('[Cron] 开始生成每日快照...');

  const portfolios = await prisma.portfolio.findMany({
    include: { transactions: true }
  });

  for (const portfolio of portfolios) {
    try {
      await generateDailySnapshot(portfolio);
      console.log(`✓ [Cron] 已生成快照: ${portfolio.name}`);
    } catch (error) {
      console.error(`✗ [Cron] 快照失败: ${portfolio.name}`, error);
    }
  }

  console.log('[Cron] 每日快照完成');
}, null, true, 'Asia/Shanghai'); // 使用上海时区

async function generateDailySnapshot(portfolio: Portfolio) {
  const today = startOfDay(new Date());

  // 检查是否已存在
  const existing = await prisma.performanceSnapshot.findUnique({
    where: {
      portfolioId_snapshotDate: {
        portfolioId: portfolio.id,
        snapshotDate: today
      }
    }
  });

  if (existing) {
    console.log(`⚠ [Snapshot] ${portfolio.name} 今日快照已存在`);
    return;
  }

  // 计算当日状态
  const positions = calculateBasePositions(portfolio.transactions);
  const quotes = await fetchQuotes(positions.map(p => p.asset.code));
  const positionsWithPnl = calculateRealtimePnl(positions, quotes);

  const totalMarketValue = positionsWithPnl.reduce((sum, p) => sum + p.marketValue, 0);
  const totalValue = portfolio.cash + totalMarketValue;

  const pnlV2 = await calculateTotalPnlV2(portfolio, positionsWithPnl);

  // 计算周期收益率
  const [dailyStats, weeklyStats, monthlyStats, yearlyStats] = await Promise.all([
    calculatePeriodStats(portfolio, 'daily', { quotes }),
    calculatePeriodStats(portfolio, 'weekly', { quotes }),
    calculatePeriodStats(portfolio, 'monthly', { quotes }),
    calculatePeriodStats(portfolio, 'yearly', { quotes })
  ]);

  // 创建快照
  await prisma.performanceSnapshot.create({
    data: {
      portfolioId: portfolio.id,
      snapshotDate: today,
      snapshotType: 'daily',
      totalValue,
      cashBalance: portfolio.cash,
      marketValue: totalMarketValue,
      totalPnl: pnlV2.totalPnl,
      realizedPnl: pnlV2.realizedPnl,
      unrealizedPnl: pnlV2.unrealizedPnl,
      dailyReturn: dailyStats.periodReturnPercent,
      weeklyReturn: weeklyStats.periodReturnPercent,
      monthlyReturn: monthlyStats.periodReturnPercent,
      yearlyReturn: yearlyStats.periodReturnPercent,
      totalReturn: (totalValue - portfolio.initialCash) / portfolio.initialCash
    }
  });
}
```

### Phase 4 执行记录（2025-11-17）

- [x] 新增 `apps/backend/src/services/__tests__/periodCacheService.test.ts`，覆盖基准价去重和周期统计缓存逻辑。
- [x] 扩展 `apps/backend/src/services/__tests__/calculationService.test.ts`，验证月度/年度基准日期、统计元数据暴露。
- [x] 前端通过 React Query 的 `usePortfolioStats` 统一复用缓存，并在 `PortfolioSummary` / `LeverageCostCard` 里接入刷新与骨架屏。
- [x] 执行 `npm run test -w apps/backend -- --runTestsByPath src/services/__tests__/calculationService.test.ts --testNamePattern "(getMonthBasePrice|getYearBasePrice|metadata)"` 验证新增用例。
- [x] 运行 `npx tsc -p frontend/tsconfig.json --noEmit` 确认前端类型检查通过；`npx tsc -p apps/backend/tsconfig.json --noEmit` 因已存在的 `verifyExchangeRate.ts` 类型问题需后续单独修复。

### 高级查询功能

```typescript
// 获取近30天收益曲线
async function getReturnCurve(portfolioId: string, days: number = 30) {
  const startDate = subDays(new Date(), days);

  const snapshots = await prisma.performanceSnapshot.findMany({
    where: {
      portfolioId,
      snapshotDate: { gte: startDate }
    },
    orderBy: { snapshotDate: 'asc' }
  });

  return snapshots.map(s => ({
    date: format(s.snapshotDate, 'yyyy-MM-dd'),
    totalValue: Number(s.totalValue),
    totalReturn: Number(s.totalReturn) * 100, // 转为百分比
    dailyReturn: Number(s.dailyReturn) * 100
  }));
}

// 对比多个投资组合
async function comparePortfolios(portfolioIds: string[], period: 'monthly' | 'yearly') {
  const snapshots = await prisma.performanceSnapshot.findMany({
    where: {
      portfolioId: { in: portfolioIds },
      snapshotType: period
    },
    orderBy: { snapshotDate: 'desc' },
    take: 12 // 最近12个周期
  });

  // 按投资组合分组
  const grouped = groupBy(snapshots, 'portfolioId');

  return portfolioIds.map(id => ({
    portfolioId: id,
    data: grouped[id] || []
  }));
}
```

### 优缺点对比

| 方面 | 缓存方案（当前） | 快照方案（未来） |
|-----|----------------|----------------|
| 实施复杂度 | ⭐ 简单 | ⭐⭐⭐ 中等 |
| 查询性能 | ⭐⭐⭐⭐ 较快 | ⭐⭐⭐⭐⭐ 极快 |
| 历史数据 | ❌ 缓存过期后丢失 | ✅ 永久保存 |
| 存储成本 | ⭐ 极低（内存） | ⭐⭐⭐ 中等（数据库） |
| 数据一致性 | ⭐⭐⭐⭐⭐ 实时计算 | ⭐⭐⭐ 定时更新（可能延迟） |
| 可扩展性 | ⭐⭐⭐ 有限 | ⭐⭐⭐⭐⭐ 极强 |
| 维护成本 | ⭐ 低 | ⭐⭐⭐ 中等（需维护定时任务） |

**推荐策略：**
1. **Phase 1-4（当前）**：实施缓存方案，快速解决问题
2. **3个月后**：评估用户需求和数据量，决定是否引入快照表
3. **6-12个月后**：基于快照数据实现高级功能（收益曲线、对比分析、报表生成）

---

## ✅ 成功标准

### 数据可用性
- [ ] 周度收益数据可用性 > 95%
- [ ] 月度收益数据可用性 > 98%
- [ ] 年度收益数据可用性 > 99%

### 性能指标
- [ ] API调用次数减少 > 90%
- [ ] 首次加载时间 < 2秒
- [ ] 后续加载时间 < 500ms
- [ ] 缓存命中率 > 90%

### 代码质量
- [ ] 单元测试覆盖率 > 80%
- [ ] 集成测试通过率 100%
- [ ] TypeScript 编译 0 errors
- [ ] ESLint 0 errors, 0 warnings

### 用户体验
- [ ] 清晰区分数据状态（缺失 vs 零涨幅）
- [ ] Tooltip 显示完整信息
- [ ] 骨架屏替代 Loading
- [ ] 错误边界友好提示

---

## ⚠️ 风险与缓解措施

### 风险1: 长期停牌股票

**问题：** 停牌超过回溯窗口（如60天），周度收益可能失真

**缓解措施：**
1. UI中明确标注"数据基于X天前的价格"
2. Tooltip 中显示实际基准日期
3. 考虑使用成本价作为最后的降级方案

### 风险2: API稳定性

**问题：** 腾讯API无官方SLA，可能限流或下线

**缓解措施：**
1. 实施请求限流和并发控制
2. 监控API响应时间和错误率
3. 准备备用数据源（新浪财经API）
4. 本地数据库缓存K线历史数据

### 风险3: 缓存一致性

**问题：** 5分钟缓存可能导致数据不够实时

**缓解措施：**
1. 提供手动刷新按钮
2. 显示数据更新时间戳
3. 对关键操作（如交易后）自动失效缓存
4. 可配置的缓存TTL（开发模式可设为0）

### 风险4: 时区和交易时间

**问题：** 不同市场的交易时间不同（A股9:30-15:00，美股21:30-次日4:00）

**缓解措施：**
1. 所有日期时间统一使用 UTC
2. 显示时转换为用户本地时区
3. 考虑市场开盘时间，避免盘中数据不准确

---

## 📚 技术参考

### 收益率计算方法

**Modified Dietz 法（当前使用）**
- 文档：[Modified Dietz Method - Wikipedia](https://en.wikipedia.org/wiki/Modified_Dietz_method)
- 适用场景：个人投资组合，不需要逐日净值
- 精度：在现金流不频繁的情况下，与TWRR接近

**时间加权收益率 (TWRR)**
- 文档：[Time-Weighted Return - Investopedia](https://www.investopedia.com/terms/t/time-weightedror.asp)
- 适用场景：基金业绩评估，消除现金流影响
- 精度：最高，但需要逐日净值数据

**资金加权收益率 (MWRR/IRR)**
- 文档：[Money-Weighted Return - Investopedia](https://www.investopedia.com/terms/m/money-weighted-return.asp)
- 适用场景：评估投资者实际回报
- 精度：反映真实收益，但受现金流时机影响

### 数据源API

**腾讯财经API（当前使用）**
```
K线数据: https://web.ifzq.gtimg.cn/appstock/app/fqkline/get?param={code},day,{startDate},{endDate},{period},{fuquan}
实时行情: https://qt.gtimg.cn/q={codes}
```

**新浪财经API（备选）**
```
K线数据: http://money.finance.sina.com.cn/quotes_service/api/json_v2.php/CN_MarketData.getKLineData
实时行情: http://hq.sinajs.cn/list={codes}
```

### 缓存库

**node-cache（当前使用）**
- GitHub: [node-cache/node-cache](https://github.com/node-cache/node-cache)
- 优点：轻量级、简单易用
- 缺点：单机内存，不支持分布式

**ioredis（可选）**
- GitHub: [luin/ioredis](https://github.com/luin/ioredis)
- 优点：支持Redis，分布式缓存
- 缺点：需要额外服务，增加复杂度

---

## 📊 附录：测试数据

### 测试股票池

| 市场 | 代码 | 名称 | 特点 |
|-----|------|------|------|
| A股 | sh600519 | 贵州茅台 | 高价股，流动性好 |
| A股 | sh600036 | 招商银行 | 大盘蓝筹 |
| A股 | sz300750 | 宁德时代 | 创业板，波动大 |
| 港股 | hk00700 | 腾讯控股 | 大市值科技股 |
| 港股 | hk09988 | 阿里巴巴-SW | ADR转港股 |
| 港股 | hk03690 | 美团-W | 新经济股 |
| 美股 | AAPL | 苹果 | 大市值科技股 |
| 美股 | MSFT | 微软 | 稳定增长 |
| 美股 | TSLA | 特斯拉 | 高波动性 |

### 假期测试场景

| 假期 | 日期范围 | 交易日停盘 | 测试要点 |
|-----|---------|-----------|---------|
| 春节 | 2025-01-28 ~ 02-03 | 7天 | 跨年长假，回溯窗口 |
| 清明 | 2025-04-04 ~ 04-06 | 3天 | 短假期 |
| 劳动节 | 2025-05-01 ~ 05-05 | 5天 | 中等假期 |
| 端午 | 2025-05-31 ~ 06-02 | 3天 | 月末假期 |
| 中秋 | 2025-10-01 ~ 10-01 | 1天 | 与国庆连休 |
| 国庆 | 2025-10-01 ~ 10-07 | 7天 | 长假，跨季度 |

---

## 📝 变更记录

| 日期 | 版本 | 变更内容 | 作者 |
|-----|------|---------|------|
| 2025-11-17 | 1.0.0 | 初始版本，完成周期收益优化方案 | AI Assistant |

---

**文档维护：** 本文档应随项目演进持续更新

**反馈渠道：** 如有问题或建议，请在项目仓库提交 Issue
