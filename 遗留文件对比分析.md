# 遗留文件清理建议对比分析

> 对比时间：2025-11-17
> 对比文档：`清理建议.md` vs 本次深度扫描报告

---

## 📊 对比总结

| 维度 | 清理建议.md | 本次深度扫描 | 差异说明 |
|------|------------|------------|---------|
| **后端遗留文件** | 6 个 | 5 个 | 基本一致，本次未发现数据库副本 |
| **前端遗留文件** | 4 个 | 14 个 | **本次发现更多 legacy 组件和测试** |
| **Electron 遗留** | 4 个 | 0 个 | 本次未涉及 launcher 方案 |
| **仍在使用的 legacy** | 已标注 | 已标注 | 一致，需要先迁移再删除 |
| **扫描深度** | 手工检查 | 自动化深度扫描 | 本次更全面系统 |

---

## ✅ 两份报告共同识别的文件（高可信度）

### 后端遗留文件（5 个完全一致）

| 文件 | 两份报告状态 | 删除优先级 |
|------|------------|----------|
| `apps/backend/src/routes/portfolio.legacy.ts` | 均确认：无引用，可删除 | **高** ✓ |
| `apps/backend/src/routes/portfolio.new.ts` | 均确认：中间版本，可删除 | **高** ✓ |
| `apps/backend/src/services/storage.deprecated.ts` | 均确认：已被 Prisma 替代 | **高** ✓ |
| `apps/backend/src/services/storage.legacy.ts` | 均确认：多余转发，可删除 | **高** ✓ |
| `packages/infra/src/storage/storage.legacy.ts` | 均确认：仍被 prisma 引用，**暂不可删** | **暂缓** ⚠️ |

### 前端遗留文件（4 个完全一致）

| 文件 | 两份报告状态 | 删除优先级 |
|------|------------|----------|
| `frontend/src/App.old.tsx.bak` | 均确认：备份文件，无引用 | **高** ✓ |
| `frontend/src/components/utils/format.ts` | 均确认：重复实现，已被 shared 替代 | **高** ✓ |
| `frontend/src/utils/formatters.ts` | 均确认：老版本，无引用 | **高** ✓ |
| `frontend/src/shared/api/client.ts` | 均确认：未使用的 typed client | **中** ⚠️ |

### 仍在使用的 Legacy（4 个一致，需迁移）

| 文件 | 引用位置 | 处理建议 |
|------|---------|---------|
| `frontend/src/components/legacy/PortfolioDetailView.tsx` | `PortfolioDetailPage.tsx` | P1 重构后删除 |
| `frontend/src/components/legacy/AddTransactionForm.tsx` | `PortfolioDetailPage.tsx` | P1 重构后删除 |
| `frontend/src/components/legacy/TransactionList.tsx` | `MarketAssetsPanel.tsx` | 迁移引用后删除 |
| `frontend/src/components/legacy/PositionsTable.tsx` | `MarketAssetsPanel.tsx` | 迁移引用后删除 |

---

## 🆕 本次深度扫描新发现的文件

### 前端 legacy 目录的完整扫描（+10 个文件）

#### 1. 额外发现的组件文件（+2 个）

| 文件 | 状态 | 说明 |
|------|------|------|
| `frontend/src/components/legacy/CreatePortfolioForm.tsx` | 无引用 | 可直接删除 |
| `frontend/src/components/legacy/PortfolioList.tsx` | 无引用 | 可直接删除 |

#### 2. 完整的测试文件目录（+6 个）

`清理建议.md` **未提及**以下测试文件，本次全部发现：

```
frontend/src/components/legacy/__tests__/
├── AddTransactionForm.test.tsx
├── CreatePortfolioForm.test.tsx
├── PortfolioDetailView.test.tsx
├── PortfolioList.test.tsx
├── PositionsTable.test.tsx
└── TransactionList.test.tsx
```

**建议**：随 legacy 组件一并删除（测试针对已废弃组件）

#### 3. 样式文件（+1 个）

| 文件 | 引用情况 | 说明 |
|------|---------|------|
| `frontend/src/components/PositionsTable.css` | 被 legacy/PositionsTable.tsx 引用 | 随 legacy 组件删除 |

---

## ⚠️ `清理建议.md` 独有但本次未发现的文件

### 1. 数据库副本文件（2 个）

| 文件 | 清理建议.md 的说明 | 本次扫描结果 |
|------|------------------|------------|
| `apps/backend/prisma/data/dev.db` | 调试/备份，可删除 | **已存在**，确认可删除 ✓ |
| `apps/backend/prisma/prisma/data/portfolio.db` | 误复制的多层路径 | **未找到该文件** ❌ |

**说明**：
- `dev.db`：本次扫描确认存在（90KB），可按建议删除或移至 backup/
- `prisma/prisma/data/`：该路径在当前项目中不存在，可能已被清理

### 2. Electron launcher 方案相关（4 个）

| 文件/目录 | 清理建议.md 的说明 | 本次扫描结果 |
|---------|------------------|------------|
| `electron/main-enhanced.ts` | 早期增强版本，未被编译 | **未找到** ❌ |
| `electron/launcher/launcher.js` | 旧启动器方案 | **已存在**，确认废弃 ⚠️ |
| `electron/launcher/package.json` | launcher 依赖 | **已存在** ⚠️ |
| `setup.iss` | Inno Setup 脚本（指向 launcher） | **已存在** ⚠️ |

**说明**：
- `main-enhanced.ts`：未找到，可能已删除
- `launcher/` 目录：存在但本次未深入分析（专注于前后端代码）
- `setup.iss`：存在于根目录，确实与当前 Electron Forge 打包不兼容

---

## 🎯 综合清理优先级（合并两份报告）

### 第一优先级：可立即删除（无任何引用）

#### 后端（4 个）
```bash
apps/backend/src/routes/portfolio.legacy.ts
apps/backend/src/routes/portfolio.new.ts
apps/backend/src/services/storage.deprecated.ts
apps/backend/src/services/storage.legacy.ts
```

#### 前端组件（2 个）
```bash
frontend/src/components/legacy/CreatePortfolioForm.tsx
frontend/src/components/legacy/PortfolioList.tsx
```

#### 前端工具（3 个）
```bash
frontend/src/App.old.tsx.bak
frontend/src/components/utils/format.ts
frontend/src/utils/formatters.ts
```

#### 前端测试（6 个）
```bash
frontend/src/components/legacy/__tests__/AddTransactionForm.test.tsx
frontend/src/components/legacy/__tests__/CreatePortfolioForm.test.tsx
frontend/src/components/legacy/__tests__/PortfolioDetailView.test.tsx
frontend/src/components/legacy/__tests__/PortfolioList.test.tsx
frontend/src/components/legacy/__tests__/PositionsTable.test.tsx
frontend/src/components/legacy/__tests__/TransactionList.test.tsx
```

**小计：15 个文件，约 2500+ 行代码**

---

### 第二优先级：需迁移引用后删除

#### 前端组件（4 个 + 1 个 CSS）
```bash
frontend/src/components/legacy/PortfolioDetailView.tsx  # → 迁移 PortfolioDetailPage.tsx
frontend/src/components/legacy/AddTransactionForm.tsx   # → 迁移 PortfolioDetailPage.tsx
frontend/src/components/legacy/TransactionList.tsx      # → 迁移 MarketAssetsPanel.tsx
frontend/src/components/legacy/PositionsTable.tsx       # → 迁移 MarketAssetsPanel.tsx
frontend/src/components/PositionsTable.css              # → 随组件删除
```

**前置条件**：完成 P1 前端状态管理/路由重构

---

### 第三优先级：评估后归档/删除

#### Electron 旧打包方案（3-4 个）
```bash
electron/launcher/                  # 整个目录（launcher.js + package.json + package-lock.json）
setup.iss                          # Inno Setup 脚本
electron/main-enhanced.ts          # （如果存在）
```

**建议**：
1. 在 `打包.md` 中明确当前使用 Electron Forge
2. 将 launcher 方案移至 `docs/archived/old-packaging-approach/`
3. 更新或删除 `setup.iss`

#### 数据库备份（1 个）
```bash
apps/backend/prisma/data/dev.db    # 移至 backup/ 或删除
```

---

### 第四优先级：暂不可删（仍有调用）

#### Infra 层存储实现（1 个）
```bash
packages/infra/src/storage/storage.legacy.ts  # 被 storage.prisma.ts 引用
```

**前置条件**：在 `storage.prisma.ts` 中完全实现历史修正与现金重算逻辑

---

## 📋 对比结论与建议

### 1. 一致性分析

两份报告在**核心遗留文件识别**上高度一致：
- ✅ 后端 5 个文件：100% 一致
- ✅ 前端核心文件：100% 一致
- ✅ 仍在使用的 legacy：100% 一致

### 2. 互补性分析

| 报告 | 优势 | 局限 |
|------|------|------|
| `清理建议.md` | 深入分析 Electron 打包方案演进 | 未覆盖 legacy 测试文件 |
| 本次深度扫描 | 自动化全面扫描，发现测试文件 | 未深入分析 Electron launcher |

### 3. 最终建议

#### 立即执行（第一批清理）

**建议执行以下一键清理脚本**：

```bash
# 后端遗留文件（4 个）
rm apps/backend/src/routes/portfolio.legacy.ts
rm apps/backend/src/routes/portfolio.new.ts
rm apps/backend/src/services/storage.deprecated.ts
rm apps/backend/src/services/storage.legacy.ts

# 前端备份和工具（3 个）
rm frontend/src/App.old.tsx.bak
rm frontend/src/components/utils/format.ts
rm frontend/src/utils/formatters.ts

# 前端无引用组件（2 个）
rm frontend/src/components/legacy/CreatePortfolioForm.tsx
rm frontend/src/components/legacy/PortfolioList.tsx

# 前端测试文件（6 个）
rm -rf frontend/src/components/legacy/__tests__/

# 清理 infra 层的 legacy export
# 手动编辑 packages/infra/src/index.ts，删除第 5 行：
# export * as legacyStorage from './storage/storage.legacy';
```

**预期效果**：删除 **15 个文件**，减少约 **2500+ 行代码**

---

#### 中期执行（第二批清理）

**前置条件**：完成前端功能模块化迁移

```bash
# 迁移以下文件的 import 从 legacy 到 features：
# - frontend/src/features/portfolio/pages/PortfolioDetailPage.tsx
# - frontend/src/components/MarketAssetsPanel.tsx

# 然后删除剩余 legacy 组件
rm frontend/src/components/legacy/PortfolioDetailView.tsx
rm frontend/src/components/legacy/AddTransactionForm.tsx
rm frontend/src/components/legacy/TransactionList.tsx
rm frontend/src/components/legacy/PositionsTable.tsx
rm frontend/src/components/PositionsTable.css
rmdir frontend/src/components/legacy  # 删除空目录
```

---

#### 长期规划（第三批清理）

1. **Electron 打包方案统一**
   ```bash
   # 归档旧方案
   mkdir -p docs/archived/old-packaging-approach
   mv electron/launcher docs/archived/old-packaging-approach/
   mv setup.iss docs/archived/old-packaging-approach/

   # 在 打包.md 中标注"仅使用 Electron Forge"
   ```

2. **数据库备份清理**
   ```bash
   # 移动到 backup 目录
   mkdir -p apps/backend/prisma/data/backup
   mv apps/backend/prisma/data/dev.db apps/backend/prisma/data/backup/
   ```

3. **Infra 层 legacy 存储**
   ```bash
   # 等 storage.prisma.ts 完全实现后
   # 删除 packages/infra/src/storage/storage.legacy.ts
   # 并清理 storage.prisma.ts 中的导入
   ```

---

## 📊 清理进度追踪

### 总计遗留文件统计

| 类别 | 文件数 | 已识别 | 可立即删除 | 需迁移后删除 | 长期规划 |
|------|-------|--------|----------|------------|--------|
| 后端遗留 | 5 | ✓ | 4 | 0 | 1 |
| 前端组件 | 8 | ✓ | 2 | 4 | 0 |
| 前端测试 | 6 | ✓ | 6 | 0 | 0 |
| 前端工具 | 4 | ✓ | 3 | 0 | 1 |
| Electron | 4 | ✓ | 0 | 0 | 4 |
| 数据库 | 1 | ✓ | 0 | 0 | 1 |
| **合计** | **28** | **28** | **15** | **4** | **7** |

### 代码减少预估

- **第一批清理**：~2,500 行（立即执行）
- **第二批清理**：~1,500 行（中期）
- **第三批清理**：~500 行（长期）
- **总计**：**~4,500 行代码**

---

## 🎯 推荐行动方案

### 方案 A：保守渐进式（推荐）

1. **本周**：执行第一批清理（15 个无引用文件）
2. **下周**：评估 Electron launcher 方案，决定归档或删除
3. **下个迭代**：完成前端迁移，执行第二批清理
4. **P1 重构后**：完成 infra 层 legacy 存储重构

### 方案 B：激进一次性清理

1. **今天**：执行第一批 + 第三批（Electron 直接删除）
2. **本周**：强制完成前端迁移，执行第二批
3. **风险**：可能破坏未记录的依赖关系

---

## 📝 检查清单

在执行清理前，请确认：

- [ ] 已备份当前代码（git commit 或 git branch）
- [ ] 已阅读两份报告的差异说明
- [ ] 已确认"仍在使用的 legacy"列表准确
- [ ] 已决定 Electron launcher 方案的处理方式
- [ ] 已通知团队成员避免并行修改
- [ ] 清理后运行完整测试套件（`npm run test`）
- [ ] 清理后验证前后端启动正常

---

## 🔗 相关文档

- 原始清理建议：`清理建议.md`
- 打包指南：`打包.md`
- 项目架构：`CLAUDE.md`
- OpenSpec 提案：`openspec/changes/evaluate-electron-packaging/`

---

**最后更新**：2025-11-17
**对比作者**：AI 深度扫描 vs 手工分析
**建议状态**：待团队审查
