# 盈亏计算优化清单

以下内容基于 `apps/backend/src/services/calculationService.ts`、`apps/backend/src/routes/portfolio.ts` 及 `packages/infra/src/storage/storage.prisma.ts` 的代码走查，分问题说明与优化步骤两部分。

## 1. 未实现盈亏 ― 持仓成本与币种

### 问题
1. `calculateBasePositions` 在卖出时直接用 `totalCost -= 成交金额`，导致历史已实现盈亏被混入剩余持仓成本。例如一笔盈利卖出后，剩余 `totalCost` 会被冲减，造成“未实现盈亏”反向漂移甚至重复统计。
2. A/H/美股的成本与市值都以各自货币累计，但 `calculateUnrealizedPnl` 直接执行 `marketValue - totalCost` 并汇总，缺少汇率换算，组合层的浮动盈亏会受到币种混算影响。

### 优化步骤
1. **改用单笔成本队列 (lot)**  
   - 为每个 `assetCode` 维护 FIFO 队列，买入时 push `{qty, costPerShareCNY}`，卖出时按 FIFO 逐笔扣减，剩余 lot 的 `totalCost` 即浮盈/亏基准。  
   - 卖出产生的 realized PnL 应当单独累加，不再写回 `totalCost`。
2. **统一 CNY 基准**  
   - 买入时即按 `amount * exchangeRate + commission * exchangeRate` 保存 CNY 成本；卖出和分红同理。  
   - `calculateBasePositions` 需读取 `transaction.exchangeRate`（若无则 fallback `getExchangeRateForAssetToCNY`），确保 positions 的 `costPrice`/`totalCost` 已是人民币。  
   - `calculateUnrealizedPnl` 汇总时即可在 CNY 维度直接相加，同时保留原币种信息以便前端展示。

## 2. 已实现盈亏 ― 汇率与手续费

### 问题
- `calculateRealizedPnl` 重新调用 `getExchangeRateForAssetToCNY`，按**当前**汇率折算历史买卖与佣金；真实的 `transaction.exchangeRate` 被忽略。  
- `calculateTotalCommission` 同样使用实时汇率，大幅偏离历史手续费成本。

### 优化步骤
1. **优先使用交易记录的 `exchangeRate`**  
   - BUY/SELL/DIVIDEND 计算时若 `tx.exchangeRate` 存在则直接使用；仅在缺失时才调用 `getExchangeRateForAssetToCNY` 并记录 warning。  
   - 缺失情况下，建议在 `correctHistoricalTransactionAmounts` 或数据迁移脚本中补录 `exchangeRate` 字段。
2. **佣金折算一致化**  
   - `calculateTotalCommission` 与 `calculateRealizedPnl` 共享一个 `getCommissionInCny(tx)` 工具函数，优先读取 `exchangeRate`。  
   - 为测试新增含 HKD/USD 交易的断言，确保手续费汇总对比人工结果。

## 3. 周期收益率 (calculatePeriodStats)

### 问题
1. `reconstructPortfolioState` 在 SELL 时用 `avgCost = cost / (qty + 卖出量)`，逻辑错误；应按剩余数量计算平均成本或同样使用 lot 模型。  
2. K 线估值环节未做汇率换算，跨币种组合的期初/期末价值失真。

### 优化步骤
1. **复用持仓重建逻辑**  
   - 将第 1 节实现的 FIFO lot 抽成 util，在 `reconstructPortfolioState` 重用，确保期初/期末持仓结构与实时视图一致。  
   - 现金余额在卖出时加收净额（收入-手续费），而不是直接累加成交金额。
2. **按历史汇率折算**  
   - 估值时，若 K 线价格为 HKD/USD，需要搭配当日汇率。可选择：  
     a) 从 `transaction.exchangeRate` 推导平均成本并把行情换算到 CNY；或  
     b) 为 `kline` 查询增加 FX 数据源（Frankfurter 支持按日查询），并缓存日期+币种对应的汇率。  
   - 期间收益率公式中的 `startValue / endValue` 统一为 CNY，再计算百分比。

## 4. 数据一致性与导入

### 问题
- 虽然 `storage.prisma` 在新增交易时会依据市场自动换算为 CNY，但历史 JSON/CSV 导入或直接写库可能绕过这层逻辑，导致 `amount`/`commission` 已经是外币，后续计算全部失真。

### 优化步骤
1. **批量导入校验**  
   - 在 `BatchImportService.validateRow` 中新增“若 currency ≠ CNY 且 exchangeRate 未填，则拒绝导入”。  
   - 导入后统一调用 `correctHistoricalTransactionAmounts`，补齐 `amount (CNY)` 与 `exchangeRate`。
2. **数据库守护**  
   - 对 `Transaction` 表增加触发器或后台 job，检测 `currency != 'CNY'` 但 `exchangeRate IS NULL` 的记录，触发自动修正并写入日志。

## 5. 自动化验证

### 优化步骤
1. 在 `apps/backend/src/services/__tests__/calculationService.test.ts` 扩展以下场景：  
   - HKD 买卖 + 分红：验证 `calculateRealizedPnl`、`calculateTotalCommission` 使用交易时汇率。  
   - Mixed currency positions：断言 `calculateUnrealizedPnl` 返回的人民币值与人工计算一致。  
   - `calculatePeriodStats` 在有卖出/入金的周期间保持与 Modified Dietz 公式一致。
2. 在 `scripts/` 目录添加一个 CLI（如 `yarn audit:pnl`），从数据库读取真实组合，输出“实时/已实现/未实现/手续费”的对比，便于上线前自检。

---

> 完成以上优化后，可重新以同一批交易数据复算，核对结果与人工 Excel 版本（含手续费与汇率）保持在「小数点后两位」以内，从而验证算法的准确性。
