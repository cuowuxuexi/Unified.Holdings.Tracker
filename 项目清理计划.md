# Unified Holdings Tracker - 项目清理计划

> 创建时间：2025-11-17
> 基于文档：`清理建议.md` + 深度扫描报告
> 状态：待执行

---

## 📋 执行概览

| 清理阶段 | 文件数 | 代码行数 | 风险等级 | 预计耗时 | 状态 |
|---------|-------|---------|---------|---------|------|
| **第一阶段：无引用文件** | 19 | ~2,800 | 🟢 低 | 30 分钟 | ✅ 已完成 |
| **第二阶段：配置清理** | 3 | ~50 | 🟢 低 | 15 分钟 | ✅ 已完成（2.2 保留待第六阶段） |
| **第三阶段：Electron 方案归档** | 4 | ~500 | 🟡 中 | 1 小时 | ⚠️ 已归档，Forge package 需联网下载依赖（当前阻塞） |
| **第四阶段：数据库清理** | 2 | 0 | 🟢 低 | 10 分钟 | ✅ 已完成 |
| **第五阶段：前端组件迁移** | 5 | ~1,500 | 🔴 高 | 2-3 天 | ⏸️ 暂缓（风险高，待迁移完成） |
| **第六阶段：Infra 层重构** | 1 | ~959 | 🔴 高 | 3-5 天 | ⏸️ 暂缓（风险高，需专项实施） |
| **合计** | **34** | **~5,809** | - | **1-2 周** | **≈ 67%** |

---

## 🎯 第一阶段：无引用文件清理（立即可执行）

### 优先级：🔴 最高
### 风险等级：🟢 低
### 执行时间：30 分钟

### 1.1 后端遗留路由和服务（4 个文件）

| # | 文件路径 | 大小 | 废弃原因 | 状态 |
|---|---------|------|---------|------|
| 1 | `apps/backend/src/routes/portfolio.legacy.ts` | 821 行 | 已被 portfolio.ts 替代，无任何导入引用 | ✅ 已删除 |
| 2 | `apps/backend/src/routes/portfolio.new.ts` | 664 行 | DI 重构中间版本，未被 server.ts 引用 | ✅ 已删除 |
| 3 | `apps/backend/src/services/storage.deprecated.ts` | 891 行 | 旧 JSON 存储，已被 Prisma 完全替代 | ✅ 已删除 |
| 4 | `apps/backend/src/services/storage.legacy.ts` | 1 行 | 多余转发文件，无任何导入 | ✅ 已删除 |

**执行命令**：
```bash
rm apps/backend/src/routes/portfolio.legacy.ts
rm apps/backend/src/routes/portfolio.new.ts
rm apps/backend/src/services/storage.deprecated.ts
rm apps/backend/src/services/storage.legacy.ts
```

**验证方法**：
```bash
# 搜索是否有残留引用
rg "portfolio\.legacy|portfolio\.new|storage\.deprecated|storage\.legacy" apps/backend/src/
# 预期：无结果
```

---

### 1.2 前端备份和工具文件（3 个文件）

| # | 文件路径 | 大小 | 废弃原因 | 状态 |
|---|---------|------|---------|------|
| 5 | `frontend/src/App.old.tsx.bak` | 225 行 | 手工备份的旧版 App 入口，无任何导入 | ✅ 已删除 |
| 6 | `frontend/src/components/utils/format.ts` | ~50 行 | 早期格式化工具，已被 shared/utils/formatters.ts 替代 | ✅ 已删除 |
| 7 | `frontend/src/utils/formatters.ts` | ~100 行 | 老版本格式化，无引用 | ✅ 已删除 |

**执行命令**：
```bash
rm frontend/src/App.old.tsx.bak
rm frontend/src/components/utils/format.ts
rm frontend/src/utils/formatters.ts
```

**验证方法**：
```bash
rg "App\.old|components/utils/format|src/utils/formatters" frontend/src/
# 预期：无结果
```

---

### 1.3 前端无引用的 legacy 组件（2 个文件）

| # | 文件路径 | 大小 | 废弃原因 | 状态 |
|---|---------|------|---------|------|
| 8 | `frontend/src/components/legacy/CreatePortfolioForm.tsx` | ~150 行 | 已被 features/portfolio/components 替代，无直接引用 | ✅ 已删除 |
| 9 | `frontend/src/components/legacy/PortfolioList.tsx` | ~200 行 | 已被 features/portfolio/components 替代，无直接引用 | ✅ 已删除 |

**执行命令**：
```bash
rm frontend/src/components/legacy/CreatePortfolioForm.tsx
rm frontend/src/components/legacy/PortfolioList.tsx
```

**验证方法**：
```bash
rg "CreatePortfolioForm|PortfolioList" frontend/src/ --type tsx
# 预期：仅在 features/ 中有新版本
```

---

### 1.4 前端 legacy 测试文件（6 个文件）

| # | 文件路径 | 说明 | 状态 |
|---|---------|------|------|
| 10-15 | `frontend/src/components/legacy/__tests__/*.test.tsx` | 针对已废弃组件的测试，共 6 个文件 | ✅ 已删除 |

**测试文件列表**：
```
frontend/src/components/legacy/__tests__/AddTransactionForm.test.tsx
frontend/src/components/legacy/__tests__/CreatePortfolioForm.test.tsx
frontend/src/components/legacy/__tests__/PortfolioDetailView.test.tsx
frontend/src/components/legacy/__tests__/PortfolioList.test.tsx
frontend/src/components/legacy/__tests__/PositionsTable.test.tsx
frontend/src/components/legacy/__tests__/TransactionList.test.tsx
```

**执行命令**：
```bash
rm -rf frontend/src/components/legacy/__tests__/
```

**验证方法**：
```bash
ls frontend/src/components/legacy/__tests__/
# 预期：No such file or directory
```

---

### 1.5 前端未使用的 API Client（1 个文件）

| # | 文件路径 | 大小 | 说明 | 状态 |
|---|---------|------|------|------|
| 16 | `frontend/src/shared/api/client.ts` | ~80 行 | 基于 generated/api 的 typed client，当前所有请求通过 services/api.ts | ✅ 已删除 |

**决策点**：
- [ ] **选项 A**：如无中长期规划使用 typed client，直接删除
- [ ] **选项 B**：移至 `experimental/` 目录，标记为"预备方案"
- [ ] **选项 C**：保留，在文档中标注"暂未启用"

**建议**：选项 A（删除），理由：
1. 当前无任何引用
2. `services/api.ts` 已稳定运行
3. 如需 typed client，可通过 OpenAPI 重新生成

**执行命令**（选择选项 A）：
```bash
rm frontend/src/shared/api/client.ts
```

---

### 第一阶段汇总

**文件总数**：16 个
**代码减少**：~2,800 行
**执行脚本**：

```bash
#!/bin/bash
# 第一阶段清理脚本

echo "开始执行第一阶段清理..."

# 1.1 后端遗留文件
echo "清理后端遗留路由和服务..."
rm apps/backend/src/routes/portfolio.legacy.ts
rm apps/backend/src/routes/portfolio.new.ts
rm apps/backend/src/services/storage.deprecated.ts
rm apps/backend/src/services/storage.legacy.ts

# 1.2 前端备份和工具
echo "清理前端备份和工具文件..."
rm frontend/src/App.old.tsx.bak
rm frontend/src/components/utils/format.ts
rm frontend/src/utils/formatters.ts

# 1.3 前端无引用组件
echo "清理前端无引用的 legacy 组件..."
rm frontend/src/components/legacy/CreatePortfolioForm.tsx
rm frontend/src/components/legacy/PortfolioList.tsx

# 1.4 前端测试文件
echo "清理前端 legacy 测试..."
rm -rf frontend/src/components/legacy/__tests__/

# 1.5 API Client（可选）
echo "清理未使用的 API Client..."
rm frontend/src/shared/api/client.ts

echo "第一阶段清理完成！"
echo "已删除 16 个文件，减少约 2,800 行代码"
```

**验证清单**：
- [x] 运行 `npm run build -w backend` - 后端构建成功
- [x] 运行 `npm run build -w frontend` - 已补充 `@rollup/rollup-linux-x64-gnu`，Vite 构建通过（输出至 `electron/renderer/`)
- [x] 运行 `npm run test -w backend` - `jest` 全量通过（存在 ts-jest deprecation 提示）
- [x] 运行 `npm run test -w frontend` - `vitest` 用例通过
- [x] 搜索残留引用：`rg "portfolio\.legacy|portfolio\.new|storage\.deprecated|storage\.legacy"`（确认仅剩文档/阶段五依赖）

---

## 🔧 第二阶段：配置和代码引用清理

### 优先级：🔴 高
### 风险等级：🟢 低
### 执行时间：15 分钟

### 2.1 清理 Infra 层的 legacy export

**文件**：`packages/infra/src/index.ts`

**当前代码**（第 5 行）：
```typescript
export * as legacyStorage from './storage/storage.legacy';
```

**操作**：删除该行

**执行步骤**：
1. 打开 `packages/infra/src/index.ts`
2. 删除第 5 行：`export * as legacyStorage from './storage/storage.legacy';`
3. 保存文件

**验证**：
```bash
rg "legacyStorage" packages/ apps/ frontend/
# 预期：无结果（除了 storage.legacy.ts 本身）
```

**状态**：✅ 已删除

---

### 2.2 清理未使用的 import

**文件**：`packages/infra/src/storage/storage.prisma.ts`

**当前代码**（第 275 行）：
```typescript
} from './storage.legacy';
```

**问题**：导入了 `storage.legacy` 但代码中未使用

**操作**：
1. 检查第 275 行附近的 import 语句
2. 如果确认未使用，删除该 import
3. 如果使用了，保留（标记为第六阶段处理）

**执行步骤**：
```bash
# 查看具体导入内容
rg -A 5 -B 5 "from './storage.legacy'" packages/infra/src/storage/storage.prisma.ts

# 检查导入的符号是否在代码中使用
# 手动删除未使用的 import
```

**状态**：⏸️ 保留（storage.prisma.ts 仍依赖 legacy 逻辑，待第六阶段重构）

---

### 2.3 检查并清理其他可能的 utils 目录

**检查点**：
```bash
# 检查是否有其他空的或重复的 utils 目录
find frontend/src -type d -name "utils" -o -name "helpers"

# 检查 components/utils/ 目录是否变空
ls frontend/src/components/utils/
```

**操作**：如果 `frontend/src/components/utils/` 只剩 `format.ts`（已删除），删除整个目录

**执行命令**（如适用）：
```bash
rmdir frontend/src/components/utils/
```

**状态**：✅ 已清理（components/utils/ 目录已删除）

---

### 第二阶段汇总

**影响文件**：3 个
**代码减少**：~50 行（主要是删除 export/import）

**执行清单**：
- [x] 删除 `packages/infra/src/index.ts` 第 5 行
- [ ] 清理 `storage.prisma.ts` 未使用的 import（已确认仍在使用，待第六阶段重构）
- [x] 检查并删除空的 `components/utils/` 目录
- [x] 验证构建：依次运行 `npm run build -w packages/domain|packages/application|packages/infra` 以及 `npm run build -w backend`

---

## 📦 第三阶段：Electron 打包方案归档

### 优先级：🟡 中
### 风险等级：🟡 中
### 执行时间：1 小时

### 背景说明

根据 `清理建议.md` 和 `openspec/changes/evaluate-electron-packaging/proposal.md`：
- 旧方案：后端独立 exe + 前端 Electron + launcher 启动器
- 新方案：Electron Forge + 内嵌后端（当前正式方案）

### 3.1 归档 Electron Launcher 目录

**目录**：`electron/launcher/`

**内容**：
- `electron/launcher/launcher.js` - 启动器脚本
- `electron/launcher/package.json` - launcher 依赖
- `electron/launcher/package-lock.json` - 锁文件

**操作**：归档到文档目录（保留历史参考）

**执行步骤**：
```bash
# 创建归档目录
mkdir -p docs/archived/old-packaging-approach

# 移动 launcher 目录
mv electron/launcher docs/archived/old-packaging-approach/

# 创建说明文档
cat > docs/archived/old-packaging-approach/README.md << 'EOF'
# 旧 Electron 打包方案（已废弃）

## 归档时间
2025-11-17

## 方案说明
该目录包含旧的 Electron 打包方案相关文件：
- launcher/ - 应用启动器（用于启动独立后端 exe + Electron 前端）
- setup.iss - Inno Setup 安装脚本

## 废弃原因
1. 依赖独立的后端 exe（portfolio-backend.exe），但该产物不再构建
2. 架构复杂，维护成本高
3. 已被 Electron Forge + 内嵌后端方案替代

## 当前方案
使用 Electron Forge 打包，后端作为 Node.js 模块直接运行在 Electron 主进程中。

详见：
- 打包指南：`../../打包.md`
- Forge 配置：`../../electron/forge.config.js`
- OpenSpec 提案：`../../openspec/changes/evaluate-electron-packaging/`

## 保留原因
作为历史参考，供未来需要独立后端部署时参考。
EOF
```

**状态**：✅ 已完成

---

### 3.2 归档或更新 Inno Setup 脚本

**文件**：`setup.iss`

**问题**：
- 当前脚本指向 `portfolio-launcher.exe`
- 与 Electron Forge 打包产物不匹配

**决策点**：
- [ ] **选项 A**：删除该脚本（Electron Forge 已提供 Squirrel 安装包）
- [ ] **选项 B**：归档到 `docs/archived/`
- [ ] **选项 C**：更新脚本以适配 Electron Forge 产物

**建议**：选项 B（归档）

**执行命令**（选择选项 B）：
```bash
mv setup.iss docs/archived/old-packaging-approach/
```

**状态**：✅ 已归档

---

### 3.3 检查 main-enhanced.ts（如存在）

**文件**：`electron/main-enhanced.ts`

**检查**：
```bash
ls electron/main-enhanced.ts
```

**操作**：
- 如果存在且未被编译，归档或删除
- 如果不存在，跳过

**执行命令**（如适用）：
```bash
mv electron/main-enhanced.ts docs/archived/old-packaging-approach/
```

**状态**：✅ 已归档

---

### 3.4 更新打包文档

**文件**：`打包.md`

**操作**：在文档中明确说明当前使用 Electron Forge，旧方案已废弃

**添加内容**（插入到"架构总览"章节）：
```markdown
## 打包方案说明

**当前方案**：Electron Forge + 内嵌后端
- 使用 Electron Forge 进行打包和分发
- 后端作为 Node.js 模块运行在主进程
- 配置文件：`electron/forge.config.js`

**历史方案**（已废弃）：
- 旧方案使用独立后端 exe + launcher 启动器
- 相关文件已归档至 `docs/archived/old-packaging-approach/`
- 废弃原因：架构复杂、维护成本高

详见：[旧方案说明](docs/archived/old-packaging-approach/README.md)
```

**状态**：✅ 已更新

---

### 第三阶段汇总

**影响文件/目录**：4 个
- `electron/launcher/` 目录（3 个文件）
- `setup.iss`
- `electron/main-enhanced.ts`（可选）

**执行清单**：
- [x] 创建归档目录 `docs/archived/old-packaging-approach/`
- [x] 移动 `electron/launcher/` 目录
- [x] 移动或删除 `setup.iss`
- [x] 检查并处理 `main-enhanced.ts`
- [x] 创建归档说明文档
- [x] 更新 `打包.md` 说明当前方案
- [ ] 验证 Electron 构建：`npm run build -w electron && cd electron && npm run package`（已运行 `npm run build -w electron`，`electron-forge package` 因离线环境无法连接 198.18.0.46:443 下载依赖而失败）

---

## 🗄️ 第四阶段：数据库文件清理

### 优先级：🟡 中
### 风险等级：🟢 低
### 执行时间：10 分钟

### 4.1 移动开发数据库到 backup

**文件**：`apps/backend/prisma/data/dev.db`

**说明**：
- 大小：90KB
- 用途：本地开发测试用 SQLite 数据库
- 状态：代码中无配置引用，仅作为调试/备份存在

**操作**：移动到 backup 目录（保留以备恢复）

**执行步骤**：
```bash
# 创建 backup 目录
mkdir -p apps/backend/prisma/data/backup

# 移动 dev.db
mv apps/backend/prisma/data/dev.db apps/backend/prisma/data/backup/

# 添加说明文件
cat > apps/backend/prisma/data/backup/README.md << 'EOF'
# 数据库备份目录

## dev.db
- 归档时间：2025-11-17
- 说明：开发环境测试数据库
- 大小：90KB
- 用途：保留以备数据恢复

## 当前使用的数据库
- 生产/开发：`../portfolio.db`
- 配置文件：`../../schema.prisma`
EOF
```

**验证**：
```bash
ls apps/backend/prisma/data/
# 预期：只有 portfolio.db 和 backup/ 目录
```

**状态**：✅ 已归档

---

### 4.2 检查多层路径的数据库副本

**文件**：`apps/backend/prisma/prisma/data/portfolio.db`

**检查**：
```bash
ls apps/backend/prisma/prisma/data/portfolio.db
```

**操作**：
- 如果存在，删除（误复制的多层路径）
- 如果不存在，跳过

**执行命令**（如适用）：
```bash
rm -rf apps/backend/prisma/prisma/
```

**状态**：✅ 已删除

---

### 4.3 更新 .gitignore

**文件**：`.gitignore` 和 `apps/backend/.gitignore`

**检查**：确保数据库文件被忽略

**当前应包含**：
```gitignore
*.db
*.db-*
```

**验证**：
```bash
cat .gitignore | grep "\.db"
cat apps/backend/.gitignore | grep "\.db"
```

**状态**：✅ 已验证（补充 apps/backend/.gitignore）

---

### 第四阶段汇总

**影响文件**：2 个（dev.db + 可能的多层路径副本）

**执行清单**：
- [x] 创建 backup 目录和说明文档
- [x] 移动 `dev.db` 到 backup
- [x] 检查并删除 `prisma/prisma/data/` 路径
- [x] 验证 .gitignore 配置
- [x] 确认当前数据库 `portfolio.db` 正常工作（`sqlite3` 查询 + `npx prisma db pull`）

---

## 🎨 第五阶段：前端 Legacy 组件迁移和删除

### 优先级：🔴 高（阻塞架构清理）
### 风险等级：🔴 高
### 执行时间：2-3 天
**当前状态**：⏸️ 暂缓（等待前端迁移工作量评估和验证）

### 前置条件
- ✅ 已完成 P1 前端状态管理重构
- ✅ 新组件已在 `frontend/src/features/` 中实现
- ✅ 新组件已通过测试

### 5.1 迁移 PortfolioDetailPage.tsx 的引用

**文件**：`frontend/src/features/portfolio/pages/PortfolioDetailPage.tsx`

**当前引用**：
```typescript
import { PortfolioDetailView } from '@/components/legacy/PortfolioDetailView';
import { AddTransactionForm } from '@/components/legacy/AddTransactionForm';
```

**目标引用**：
```typescript
import { PortfolioDetailView } from '@/features/portfolio/components/PortfolioDetailView';
import { AddTransactionForm } from '@/features/transaction/components/AddTransactionForm';
```

**执行步骤**：
1. 打开 `PortfolioDetailPage.tsx`
2. 更新 import 路径
3. 验证组件 props 兼容性
4. 测试页面功能

**验证方法**：
```bash
npm run dev:frontend
# 访问组合详情页，测试所有功能：
# - 显示组合信息
# - 添加交易
# - 编辑交易
# - 删除交易
```

**状态**：⬜ 待执行

---

### 5.2 迁移 MarketAssetsPanel.tsx 的引用

**文件**：`frontend/src/components/MarketAssetsPanel.tsx`

**当前引用**：
```typescript
import { TransactionList } from './legacy/TransactionList';
import { PositionsTable } from './legacy/PositionsTable';
```

**目标引用**：
```typescript
import { TransactionList } from '@/features/transaction/components/TransactionList';
import { PositionsTable } from '@/features/portfolio/components/PositionsTable';
```

**执行步骤**：
1. 打开 `MarketAssetsPanel.tsx`
2. 更新 import 路径
3. 验证组件 props 兼容性
4. 测试面板功能

**验证方法**：
```bash
npm run dev:frontend
# 访问市场资产面板，测试：
# - 持仓表格显示
# - 交易列表显示
# - 数据刷新
```

**状态**：⬜ 待执行

---

### 5.3 删除剩余的 Legacy 组件

**文件列表**（4 个组件 + 1 个样式）：
```
frontend/src/components/legacy/PortfolioDetailView.tsx
frontend/src/components/legacy/AddTransactionForm.tsx
frontend/src/components/legacy/TransactionList.tsx
frontend/src/components/legacy/PositionsTable.tsx
frontend/src/components/PositionsTable.css
```

**执行步骤**：
```bash
# 删除组件文件
rm frontend/src/components/legacy/PortfolioDetailView.tsx
rm frontend/src/components/legacy/AddTransactionForm.tsx
rm frontend/src/components/legacy/TransactionList.tsx
rm frontend/src/components/legacy/PositionsTable.tsx

# 删除样式文件
rm frontend/src/components/PositionsTable.css

# 删除空的 legacy 目录
rmdir frontend/src/components/legacy
```

**验证方法**：
```bash
# 确保无残留引用
rg "components/legacy" frontend/src/

# 确保构建成功
npm run build -w frontend

# 运行前端测试
npm run test -w frontend
```

**状态**：⬜ 待执行

---

### 5.4 验证新组件完整性

**检查清单**：
- [ ] `frontend/src/features/portfolio/components/PortfolioDetailView.tsx` - 功能完整
- [ ] `frontend/src/features/transaction/components/AddTransactionForm.tsx` - 功能完整
- [ ] `frontend/src/features/transaction/components/TransactionList.tsx` - 功能完整
- [ ] `frontend/src/features/portfolio/components/PositionsTable.tsx` - 功能完整
- [ ] 所有新组件已有对应测试文件
- [ ] 所有新组件已集成到路由

**验证方法**：
```bash
# 检查新组件是否存在
ls frontend/src/features/portfolio/components/PortfolioDetailView.tsx
ls frontend/src/features/transaction/components/AddTransactionForm.tsx
ls frontend/src/features/transaction/components/TransactionList.tsx
ls frontend/src/features/portfolio/components/PositionsTable.tsx

# 检查测试文件
ls frontend/src/features/portfolio/components/__tests__/
ls frontend/src/features/transaction/components/__tests__/
```

**状态**：⬜ 待验证

---

### 第五阶段汇总

**影响文件**：7 个（2 个页面引用 + 5 个 legacy 组件/样式）
**代码减少**：~1,500 行

**执行清单**：
- [ ] 迁移 `PortfolioDetailPage.tsx` 的 import
- [ ] 迁移 `MarketAssetsPanel.tsx` 的 import
- [ ] 验证新组件功能完整
- [ ] 删除 4 个 legacy 组件文件
- [ ] 删除 `PositionsTable.css`
- [ ] 删除空的 `legacy/` 目录
- [ ] 完整回归测试
- [ ] 更新 CLAUDE.md 中的组件架构说明

**回归测试清单**：
- [ ] 组合列表页正常显示
- [ ] 创建组合功能正常
- [ ] 组合详情页正常显示
- [ ] 添加交易功能正常
- [ ] 编辑交易功能正常
- [ ] 删除交易功能正常
- [ ] 持仓表格正常显示
- [ ] 交易列表正常显示
- [ ] 市场数据正常更新

---

## 🏗️ 第六阶段：Infra 层 Legacy 存储重构

### 优先级：🟡 中（长期改进）
### 风险等级：🔴 高
### 执行时间：3-5 天
**当前状态**：⏸️ 暂缓（需专项数据验证与设计）

### 背景说明

**文件**：`packages/infra/src/storage/storage.legacy.ts`

**当前状态**：
- 标记为"遗留 JSON 存储（废弃）"
- 仍被 `storage.prisma.ts` 引用
- 提供历史修正与现金重算逻辑

**问题**：
```typescript
// packages/infra/src/storage/storage.prisma.ts 第 275 行
import {
  recalculateCashTransactions,
  fixHistoricalData
} from './storage.legacy';
```

### 6.1 分析 Legacy 存储的依赖

**执行步骤**：
```bash
# 查找所有对 storage.legacy 的引用
rg "storage\.legacy" packages/infra/src/

# 分析 storage.prisma.ts 中使用的函数
rg "recalculateCashTransactions|fixHistoricalData" packages/infra/src/storage/storage.prisma.ts -A 10
```

**输出**：记录所有使用 legacy 函数的位置和上下文

**状态**：⬜ 待分析

---

### 6.2 在 Prisma 存储中重新实现逻辑

**目标**：在 `storage.prisma.ts` 中实现以下功能：
1. `recalculateCashTransactions` - 现金交易重算
2. `fixHistoricalData` - 历史数据修正

**执行步骤**：
1. 阅读 `storage.legacy.ts` 中的实现逻辑
2. 使用 Prisma API 重新实现
3. 编写单元测试验证新实现
4. 在开发环境测试数据修正功能

**参考实现**（伪代码）：
```typescript
// packages/infra/src/storage/storage.prisma.ts

/**
 * 重算现金交易（Prisma 实现）
 */
async function recalculateCashTransactions(portfolioId: number): Promise<void> {
  const portfolio = await prisma.portfolio.findUnique({
    where: { id: portfolioId },
    include: { transactions: true }
  });

  // 实现重算逻辑...
}

/**
 * 修正历史数据（Prisma 实现）
 */
async function fixHistoricalData(): Promise<void> {
  const portfolios = await prisma.portfolio.findMany({
    include: { transactions: true }
  });

  // 实现修正逻辑...
}
```

**状态**：⬜ 待实现

---

### 6.3 编写迁移测试

**测试文件**：`packages/infra/src/storage/__tests__/storage.prisma.test.ts`

**测试用例**：
1. 测试现金交易重算功能
2. 测试历史数据修正功能
3. 对比新旧实现的结果一致性

**执行步骤**：
```bash
# 运行测试
npm run test -w infra

# 验证覆盖率
npm run test:coverage -w infra
```

**状态**：⬜ 待编写

---

### 6.4 更新引用并删除 legacy 存储

**执行步骤**：
1. 在 `storage.prisma.ts` 中删除 legacy import
2. 使用新实现替换所有调用
3. 删除 `storage.legacy.ts` 文件
4. 运行完整测试套件

**执行命令**：
```bash
# 删除 legacy 文件
rm packages/infra/src/storage/storage.legacy.ts

# 验证无残留引用
rg "storage\.legacy" packages/

# 构建并测试
npm run build -w infra
npm run test -w infra
npm run build -w backend
npm run test -w backend
```

**状态**：⬜ 待执行

---

### 第六阶段汇总

**影响文件**：1 个（`storage.legacy.ts`）
**代码减少**：~959 行
**代码新增**：~200 行（Prisma 实现 + 测试）

**执行清单**：
- [ ] 分析 legacy 存储依赖
- [ ] 在 Prisma 存储中实现 `recalculateCashTransactions`
- [ ] 在 Prisma 存储中实现 `fixHistoricalData`
- [ ] 编写单元测试
- [ ] 在开发环境验证功能
- [ ] 删除 `storage.legacy.ts`
- [ ] 清理 import 引用
- [ ] 运行完整测试套件
- [ ] 更新 Infra 层 CLAUDE.md 文档

**风险控制**：
- 🔴 **高风险**：涉及数据修正逻辑，错误可能导致数据损坏
- 📋 **建议**：
  1. 在测试数据库上先验证
  2. 备份生产数据库
  3. 保留 `storage.legacy.ts` 副本作为参考（移至 `docs/archived/`）
  4. 分阶段部署（先内部测试，再正式发布）

---

## 📊 总体进度追踪

### 清理进度表

| 阶段 | 文件数 | 状态 | 开始日期 | 完成日期 | 负责人 | 验证人 |
|------|-------|------|---------|---------|-------|-------|
| 第一阶段：无引用文件 | 16 | ✅ 已完成 | 2025-11-17 | 2025-11-17 | - | - |
| 第二阶段：配置清理 | 3 | ✅ 已完成（2.2 待后续收尾） | 2025-11-17 | 2025-11-17 | - | - |
| 第三阶段：Electron 归档 | 4 | ⚠️ 已归档，Forge package 需联网（待恢复） | 2025-11-17 | 2025-11-17 | - | - |
| 第四阶段：数据库清理 | 2 | ✅ 已完成 | 2025-11-17 | 2025-11-17 | - | - |
| 第五阶段：前端迁移 | 7 | ⏸️ 暂缓（高风险，等待迁移方案） | - | - | - | - |
| 第六阶段：Infra 重构 | 1 | ⏸️ 暂缓（高风险，需专项实施） | - | - | - | - |
| **合计** | **33** | **≈ 67%** | - | - | - | - |

---

## 🎯 推荐执行顺序

### Week 1: 低风险清理

**第 1 天**：
- ✅ 执行第一阶段（16 个文件）
- ✅ 执行第二阶段（配置清理）
- ✅ 验证构建和测试

**第 2-3 天**：
- ✅ 执行第三阶段（Electron 归档）
- ✅ 执行第四阶段（数据库清理）
- ✅ 更新文档

**第 4-5 天**：
- ✅ Review 清理结果
- ✅ 完整回归测试
- ✅ 提交 PR

---

### Week 2-3: 前端重构

**Week 2**：
- ✅ 迁移 `PortfolioDetailPage.tsx`
- ✅ 迁移 `MarketAssetsPanel.tsx`
- ✅ 验证新组件功能

**Week 3**：
- ✅ 删除 legacy 组件
- ✅ 完整回归测试
- ✅ 提交 PR

---

### Week 4-5: Infra 重构（可选）

**Week 4**：
- ✅ 分析 legacy 存储依赖
- ✅ 实现 Prisma 版本
- ✅ 编写测试

**Week 5**：
- ✅ 验证功能
- ✅ 删除 legacy 存储
- ✅ 提交 PR

---

## ✅ 验证和测试策略

### 单元测试

**每个阶段完成后运行**：
```bash
# 后端测试
npm run test -w backend

# 前端测试
npm run test -w frontend

# Infra 测试
npm run test -w infra
```

---

### 集成测试

**第一、二、三、四阶段完成后**：
```bash
# 构建所有包
npm run build:packages

# 构建后端
npm run build -w backend

# 构建前端
npm run build -w frontend

# 启动后端
npm run dev:backend

# 启动前端（新终端）
npm run dev:frontend

# 验证核心功能
# - 创建组合
# - 添加交易
# - 查看持仓
# - 市场数据更新
```

---

### 端到端测试

**第五阶段完成后**：
```bash
# Electron 完整打包测试
npm run build:all
cd electron
npm run package

# 测试打包的应用
# - 启动应用
# - 完整功能测试
# - 性能测试
```

---

### 回归测试清单

#### 后端 API
- [ ] GET /api/portfolios - 获取组合列表
- [ ] POST /api/portfolios - 创建组合
- [ ] GET /api/portfolios/:id - 获取组合详情
- [ ] PUT /api/portfolios/:id - 更新组合
- [ ] DELETE /api/portfolios/:id - 删除组合
- [ ] GET /api/portfolios/:id/transactions - 获取交易列表
- [ ] POST /api/portfolios/:id/transactions - 添加交易
- [ ] PUT /api/transactions/:id - 更新交易
- [ ] DELETE /api/transactions/:id - 删除交易
- [ ] GET /api/market/quote/:symbol - 获取行情

#### 前端功能
- [ ] 组合列表页显示
- [ ] 创建组合表单
- [ ] 组合详情页显示
- [ ] 持仓表格显示
- [ ] 添加交易表单
- [ ] 编辑交易功能
- [ ] 删除交易功能
- [ ] 交易列表显示
- [ ] 市场数据更新
- [ ] 图表显示

#### Electron 应用
- [ ] 应用启动正常
- [ ] 主窗口显示正常
- [ ] 前后端通信正常
- [ ] 数据持久化正常
- [ ] 关闭应用数据保存

---

## 🚨 风险控制和回滚计划

### 风险识别

| 风险 | 等级 | 影响 | 缓解措施 |
|------|------|------|---------|
| 删除仍在使用的文件 | 🔴 高 | 构建失败 | 验证引用、分阶段删除 |
| 前端组件迁移失败 | 🔴 高 | 功能缺失 | 完整测试、保留 legacy |
| Infra 重构数据损坏 | 🔴 高 | 数据丢失 | 备份数据库、测试环境验证 |
| 打包配置冲突 | 🟡 中 | 打包失败 | 归档旧配置、文档说明 |
| 测试覆盖不足 | 🟡 中 | 遗漏 bug | 增加测试、手动回归 |

### 回滚计划

**第一阶段回滚**：
```bash
git checkout HEAD -- apps/backend/src/routes/portfolio.legacy.ts
git checkout HEAD -- apps/backend/src/routes/portfolio.new.ts
# ... 恢复其他文件
```

**第二阶段回滚**：
```bash
git checkout HEAD -- packages/infra/src/index.ts
git checkout HEAD -- packages/infra/src/storage/storage.prisma.ts
```

**第五阶段回滚**：
```bash
git checkout HEAD -- frontend/src/components/legacy/
git checkout HEAD -- frontend/src/features/portfolio/pages/PortfolioDetailPage.tsx
git checkout HEAD -- frontend/src/components/MarketAssetsPanel.tsx
```

**第六阶段回滚**：
```bash
git checkout HEAD -- packages/infra/src/storage/storage.legacy.ts
git checkout HEAD -- packages/infra/src/storage/storage.prisma.ts
```

### Git 分支策略

**建议**：每个阶段使用独立分支
```bash
# 第一阶段
git checkout -b cleanup/phase-1-unused-files

# 第二阶段
git checkout -b cleanup/phase-2-config

# 第三阶段
git checkout -b cleanup/phase-3-electron

# 第四阶段
git checkout -b cleanup/phase-4-database

# 第五阶段
git checkout -b cleanup/phase-5-frontend

# 第六阶段
git checkout -b cleanup/phase-6-infra
```

---

## 📝 文档更新清单

### 需要更新的文档

1. **CLAUDE.md**
   - [ ] 更新模块结构图（删除 legacy 引用）
   - [ ] 更新组件架构说明
   - [ ] 更新打包方案说明

2. **打包.md**
   - [ ] 添加 Electron Forge 说明
   - [ ] 标注旧方案已废弃
   - [ ] 更新构建流程

3. **packages/*/CLAUDE.md**
   - [ ] domain/CLAUDE.md - 无需更新
   - [ ] application/CLAUDE.md - 无需更新
   - [ ] infra/CLAUDE.md - 第六阶段完成后更新存储说明

4. **apps/backend/CLAUDE.md**
   - [ ] 更新路由说明（删除 legacy 引用）
   - [ ] 更新存储服务说明

5. **frontend/CLAUDE.md**
   - [ ] 更新组件架构说明
   - [ ] 删除 legacy 组件引用
   - [ ] 更新功能模块说明

6. **electron/CLAUDE.md**（如有）
   - [ ] 更新打包方案说明
   - [ ] 删除 launcher 引用

---

## 🎉 完成标准

### 第一阶段完成标准
- ✅ 16 个文件已删除
- ✅ 无残留引用（rg 搜索结果为空）
- ✅ 后端构建成功
- ✅ 前端构建成功
- ✅ 测试通过

### 第二阶段完成标准
- ✅ legacy export 已删除
- ✅ 未使用 import 已清理
- ✅ 空目录已删除
- ✅ 构建成功

### 第三阶段完成标准
- ✅ launcher 目录已归档
- ✅ setup.iss 已处理
- ✅ 归档说明文档已创建
- ✅ 打包.md 已更新
- ✅ Electron 打包测试成功

### 第四阶段完成标准
- ✅ dev.db 已移至 backup
- ✅ 多层路径副本已删除
- ✅ .gitignore 配置正确
- ✅ 当前数据库正常工作

### 第五阶段完成标准
- ✅ 所有页面引用已迁移
- ✅ legacy 组件已删除
- ✅ legacy 目录已删除
- ✅ 前端构建成功
- ✅ 完整回归测试通过

### 第六阶段完成标准
- ✅ Prisma 实现功能完整
- ✅ 测试覆盖率 > 80%
- ✅ storage.legacy.ts 已删除
- ✅ 无残留引用
- ✅ 数据修正功能验证通过

### 整体完成标准
- ✅ 所有 6 个阶段完成
- ✅ 33+ 个文件已删除/归档
- ✅ 代码减少 ~5,800 行
- ✅ 所有文档已更新
- ✅ 完整测试套件通过
- ✅ Electron 打包测试成功
- ✅ 团队 Review 通过

---

## 📞 联系和支持

### 问题反馈
如在执行过程中遇到问题，请：
1. 记录详细错误信息
2. 记录当前执行到的步骤
3. 创建 Issue 或联系团队

### 需要帮助时
- 查看本文档的"回滚计划"
- 查看 Git 历史记录
- 查看相关 CLAUDE.md 文档

---

**文档版本**：1.0
**创建日期**：2025-11-17
**最后更新**：2025-11-17
**维护者**：开发团队
**状态**：待审查和执行
