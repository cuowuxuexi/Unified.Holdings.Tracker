
## 一、后端可疑遗留 / 重复文件

- `apps/backend/src/routes/portfolio.legacy.ts`
  - 说明：旧版组合路由，当前入口使用 `apps/backend/src/routes/portfolio.ts`，`server.ts` 只导入该文件。
  - 状态：代码中无任何导入引用，仅在文档中作为备份提及。
  - 建议：整体删除或移动到 `docs/` / `archive/` 目录，作为历史参考。

- `apps/backend/src/routes/portfolio.new.ts`
  - 说明：DI 重构过程中的中间版本路由实现。
  - 状态：同样未被 `server.ts` 或其他路由注册逻辑引用。
  - 建议：与 `portfolio.legacy.ts` 一起归档或删除，避免混淆当前正式路由实现。

- `apps/backend/src/services/storage.deprecated.ts`
  - 说明：基于 JSON 文件的旧版存储实现。
  - 状态：`rg` 检索无任何调用方，当前业务已经统一通过 Prisma 存储实现（`packages/infra/src/storage/storage.prisma.ts`）。
  - 建议：作为“已完全替换”的实现，建议删除或归档。

- `apps/backend/src/services/storage.legacy.ts`
  - 说明：仅一行 `export * from '@uht/infra/storage/storage.legacy';`，用于转发 infra 层 legacy 存储。
  - 状态：后端代码中没有任何导入该模块的地方；真正使用 legacy 存储的地方在 `packages/infra/src/storage/storage.prisma.ts` 内部直接引用 `./storage.legacy`。
  - 建议：删除该“多余转发”文件，减少命名上的混淆。

- `apps/backend/prisma/data/dev.db`
  - 说明：Prisma 本地开发测试用 SQLite 数据库。
  - 状态：代码和配置中没有直接引用该文件名，仅作为调试/备份存在。
  - 建议：如果确认不再需要该数据库快照，可删除；否则建议移入 `apps/backend/prisma/data/backup/` 之类目录，并在文档中标注用途。

- `apps/backend/prisma/prisma/data/portfolio.db`
  - 说明：路径多了一层 `prisma/` 的数据库副本。
  - 状态：`DATABASE_URL` 默认指向 `./data/portfolio.db` 或 `./prisma/data/portfolio.db`，没有任何配置指向该路径，极大概率是误复制。
  - 建议：确认后删除，避免未来路径配置时误用。

## 二、前端可疑遗留 / 重复文件

- `frontend/src/App.old.tsx.bak`
  - 说明：手工备份的旧版 App 入口。
  - 状态：未被任何地方导入。
  - 建议：直接删除；如需保留历史代码，建议搬运到 `docs/` 或 git tag 中。

- `frontend/src/components/utils/format.ts`
  - 说明：早期的百分比格式化工具。
  - 状态：没有任何引用；当前格式化逻辑统一走 `frontend/src/shared/utils/formatters.ts`。
  - 建议：删除该重复实现，避免后续出现两套格式化逻辑。

- `frontend/src/utils/formatters.ts`
  - 说明：老版本通用格式化工具（`formatDate`、`formatNumber`、`formatPercent`）。
  - 状态：仓库中没有任何导入引用；新实现位于 `frontend/src/shared/utils/formatters.ts`，并新增了 `formatCurrency`。
  - 建议：删除该文件；如有历史依赖，可在变更说明中补充“已统一迁移到 shared/utils/formatters.ts”。

- `frontend/src/shared/api/client.ts`
  - 说明：基于 `generated/api` 的 typed client 封装。
  - 状态：当前所有请求均通过 `frontend/src/services/api.ts` 进行，`shared/api/client` 无任何导入引用。
  - 建议：如果短期内没有计划切换到统一 typed client，可以删除该文件及相关导出；如有中长期规划，可在文档中标记为“暂不启用的预备方案”，并移动到 `experimental/`。

## 三、Electron / 打包相关历史实现

- `electron/main-enhanced.ts`
  - 说明：主进程的早期增强版本。
  - 状态：`electron/tsconfig.json` 的 `include` 只包含 `main.ts`，该文件不会被编译，也没有任何导入引用。
  - 建议：作为完全脱离构建链路的历史文件，可以删除或归档到 `electron/legacy/`。

- `electron/launcher/launcher.js`
- `electron/launcher/package.json`
- `electron/launcher/package-lock.json`
  - 说明：对应“后端独立 exe + 前端 Electron + 启动器”的旧方案，通过 `portfolio-launcher.exe` 启动前后端。
  - 状态：当前主线打包方案是 Electron Forge + 内嵌后端；`openspec/changes/evaluate-electron-packaging/proposal.md` 已将该方案标记为“旧方案，依赖不存在的独立 exe”，代码中也未再引用 launcher。
  - 建议：如果未来不再使用 launcher 路径，可整体删除；如需保留经验，可迁移到 `docs/notes/` 或 `electron/legacy/launcher/` 并在文档中明确“仅供参考，不再维护”。

- `setup.iss`
  - 说明：Inno Setup 安装脚本，目前仍指向 `portfolio-launcher.exe`。
  - 状态：与现有 Electron Forge 打包产物不匹配，属于旧方案残留。
  - 建议：与 `electron/launcher/` 同步决策——要么更新脚本以适配新产物，要么与旧方案一起归档/删除。

## 四、“仍在使用的 legacy 代码（暂不可删）”

- 前端 legacy 组件（仍在运行路径）
  - `frontend/src/components/legacy/PortfolioDetailView.tsx`
  - `frontend/src/components/legacy/AddTransactionForm.tsx`
    - 被 `frontend/src/features/portfolio/pages/PortfolioDetailPage.tsx` 显式引入，并在注释中标记为“临时使用 legacy 组件”，页面仍依赖。
  - `frontend/src/components/legacy/TransactionList.tsx`
  - `frontend/src/components/legacy/PositionsTable.tsx`
    - 被 `frontend/src/components/MarketAssetsPanel.tsx` 引用，用于展示行情与持仓。
  - 建议：在完成 P1 前端状态管理/路由重构后，逐步将页面切换到 `frontend/src/features/portfolio` 和 `frontend/src/features/transaction` 下的新组件，再清理 legacy 目录。

- 后端 legacy 存储实现（infra 层复用）
  - `packages/infra/src/storage/storage.legacy.ts`
    - 虽然在 `packages/infra/CLAUDE.md` 中被标为“遗留 JSON 存储（废弃）”，但当前仍被 `packages/infra/src/storage/storage.prisma.ts` 通过 `import { ... } from './storage.legacy'` 复用部分交易逻辑（历史修正与现金重算）。
  - 建议：在新逻辑中完全实现对应能力、移除对 legacy 模块的 import 后，再考虑删除；在此之前，仅进行注释标记“仍有调用方，禁止删除”。

## 五、推荐清理顺序

1. **优先删除无任何代码引用的纯备份文件**  
   - 如：`frontend/src/App.old.tsx.bak`、`frontend/src/components/utils/format.ts`、`frontend/src/utils/formatters.ts`、`apps/backend/src/services/storage.deprecated.ts` 等。

2. **清理后端/数据库的明显重复数据文件**  
   - 针对 `apps/backend/prisma/prisma/data/portfolio.db` 等多余副本，删除前可以先备份到 `backup/` 目录，并在文档中注明。

3. **评估并归档旧打包方案相关文件**  
   - 对 `electron/launcher/*` 和 `setup.iss`，在 README/打包文档中明确当前官方推荐的打包流程后，将旧方案整体迁移到 `legacy/` 或删除。

4. **在功能迁移完成后，统一收口 legacy 组件与存储实现**  
   - 等前端页面和后端交易逻辑完全切换到新实现后，再做第二轮“有调用方的 legacy 代码”清理。
